# Tale Dives — Project Revision Notes

**Read this file first, always.** It carries the current state, the "read this before touching X" warnings, and the pending-work list — everything a session actually needs to resume work. 

For the complete historical log of past decisions and older entries, see [`PROJECT_REVISION_NOTES_ARCHIVE.md`](./PROJECT_REVISION_NOTES_ARCHIVE.md).

## 0. How to resume
- **Start the dev server:** `npm run dev` (runs on `0.0.0.0:3000` per `package.json`).
- **Check the preview:** Open the shared URL or dev URL.
- **Run the checks:** `npm run typecheck` and `npm run build` must pass clean.

## 1. What Tale Dives Is
An immersive, LLM-driven fantasy novel game. The protagonist's actions and the world's responses are generated as narrative prose by Gemini, but long-term memory, game state (inventory, codex, NPCs, factions, quests), and mechanics are tracked deterministically in client-side TypeScript structures and parsed from the LLM's structured `<sync>` output each turn. It runs purely client-side (React + Vite) with no backend, storing state in `localStorage` with optional Google Drive backup.

## 2. Current File Inventory
- `src/App.tsx`: Main router, global state (`game`, `campaigns`), turn loop manager (`sendAction`).
- `src/screens/`: Major UI views (`Title`, `MainMenu`, `Chronicle`, `Codex`, `TaleWeaver`, etc.).
- `src/components/`: Sub-components (`glassChrome.tsx`, `seedweaver/`).
- `src/lib/`: Core logic (`taleWeaving.ts`, `taleWeaverParser.ts`, `store.ts`, `jitContext.ts`, `xmlTurnParser.ts`, `seeding.ts`, `npcs.ts`, `quests.ts`, `imageGeneration.ts`, etc.).
- `src/types.ts`: Master types (Campaign, TurnResponse, Codex entries).
- `src/api/`: Gemini API integration and turn/seeding contracts (`providers/gemini.ts`, `xmlTurnContract.ts`, `taleWeaverContract.ts`).
- `src/data/`: Static data (`classes.ts`, `starterTemplates.ts`, `soundtrackManifest.ts`).
- `PROJECT_REVISION_NOTES_ARCHIVE.md`: The full historical log.

## 3. What's Built (Current State)
- **Tale Weaving (Inspired Mode):** A phased, generative setup flow (World → Protagonist → Cast → Lore → Arc) using the LLM to draft a rich starting state. Includes safety confirmations on generation. Tale Weaving's Arc phase also captures Narrative Events and Death/End Game Rules interactively, and world seeding (Original Mode, `lib/seeding.ts`) generates starter Narrative Events too.
- **Narrative Events:** `Campaign.narrativeEvents?: Dict<NarrativeEvent>` — condition-triggered reactive story complications (distinct from the linear `Campaign.beats[]` spine), checked client-side each turn via `lib/narrativeEvents.ts` against the same trigger vocabulary Codex Discovery uses (`flag`/`location_visit`/`npc_met`/`quest_complete`/`manual`), plus a `story` trigger where the model itself emits `<event_trip id="..." />` when a freeform prose condition is met.
- **Death Rules & End Game Rules:** `Campaign.deathRule?: 'soft_fail' | 'permadeath'` (plus `deathInstructions`) branches defeat handling between the existing recovery beat and a real story-ending death; `Campaign.endGameRules?: Partial<Record<EndingOutcome, string>>` is per-outcome narration guidance surfaced to the model on the existing `!conclude`/final-beat ending triggers.
- **NPC Kinship:** `KinshipType` (`'parent'`/`'sibling'`/`'child'`/`'spouse'`/`'mentor'`/`'clan'`) on `NpcEntry.kinship`, hard-gating INTIMACY-track escalation for family relations regardless of Trust/Affection tier.
- **XML Turn Engine:** The narrator emits a `<nar>` block for prose and a `<sync>` block (parsed by `xmlTurnParser.ts`) containing exact state updates (`<loc>`, `<npc>`, `<item>`, `<enrich>`, `<project>`, `<breakthrough>`, etc.).
- **JIT Context:** Slices the massive Codex into a small ~40-80 token header (`jitContext.ts`) injected into the prompt each turn, based on who is present or relevant.
- **Codex & Discovery:** Tracks Lore, NPCs, Factions, Locations, Items, Quests, Skills, Projects, and Bestiary. Items are gated by `DiscoveryState` (Known vs. Hidden).
- **Client-Side Image Generation:** Generates UI assets (character portraits, realm art) on the fly via Gemini (`lib/imageGeneration.ts`), storing them in IndexedDB.
- **Background Music:** Context-aware soundtrack crossfading based on turn states.
- **Data Persistence:** Client-side `localStorage` with multiple campaign slots and optional Google Drive backup/restore.

## 4. What's Not Built / Known Issues
- Full multi-device sync or backend database (intentionally client-side for now).
- Novel Weaver (a second, alternate Tale-creation flow) was built earlier and has since been fully removed — don't reintroduce it or look for `screens/NovelWeaver.tsx`, it no longer exists.

## 5. Explicitly Requested But Not Started
- Action Suggestion Pills (rendering clickable `turn.act` suggestions into the UI).

## 6. Suggested Resumption Order
- Pick up from the user's latest prompt. Always verify changes with a live click-through in the dev server.

## Full revision history
Every dated session entry through 2026-09-09 has been moved to [`PROJECT_REVISION_NOTES_ARCHIVE.md`](./PROJECT_REVISION_NOTES_ARCHIVE.md). That file is a verbatim continuation of the same log; nothing was edited or condensed, only relocated. Read it only when a specific past decision needs more detail than the summary sections above give — for resuming work, everything above this line is what actually matters.

### New entries below, most recent first.

- **2026-09-10** — Parchment Popup Images & Champagne Gold Premium Image Generation (`src/screens/Chronicle.tsx`, `src/screens/TaleWeaver.tsx`, `src/screens/Codex.tsx`, `src/lib/imageGeneration.ts`, `src/lib/entityImages.ts`, `src/types.ts`, `.env.example`).
  1. **Parchment Keyword Popup Image Display:** Parchment keyword popups in `Chronicle.tsx` now check for entity image keys (`portraitKey`, `imageKey`, `mapImageKey`) via `useEntityImage` and display the generated artwork in a styled container inside the parchment popup card.
  2. **Premium Nanobanana Key & Champaign Gold Generation Button:** Added `VITE_GEMINI_PREM_KEY` support in `.env.example`, `ApiSettings.premiumApiKey` in `types.ts`, and `getPremiumApiKey` helper in `lib/imageGeneration.ts`. Added a Champagne Gold `'Premium'` button in `TaleWeaverImageGenerator` (`TaleWeaver.tsx`) and `EntityImagePanel` (`Codex.tsx`) alongside a warning note that 'Premium' uses paid API tokens (`Gemini_Prem_Key` quota).
  3. Verified clean build and execution via `compile_applet`.

- **2026-09-10** — Codex Navigation Return Fix & Region Deduplication (`src/screens/Codex.tsx`, `src/screens/Chronicle.tsx`, `src/lib/codex.ts`, `src/App.tsx`). Fixed Codex navigation bug where opening a Codex entry from the Parchment view (`Chronicle`) didn't return to the Parchment view upon pressing back. `Codex.tsx` now tracks `openedWithTargetRef` and calls `onBack()` directly to return to caller view (`Chronicle`). Solved the redundant "Basgiath War College" location bug where a region was auto-logged as an empty location stub: `applyKeywordLinks` now checks `regions` before auto-logging a location stub, `dedupLocationsWithRegions` purges empty location stubs that duplicate known Region entries, and `Chronicle` / `App` smoothly fallback to Region entries for location keyword links. Verified clean build and execution.

- **2026-09-10** — Initial Prompt Edit Preview on First Click (`src/screens/TaleWeaver.tsx`, `src/screens/Codex.tsx`). Updated `handleButtonClick` in `TaleWeaverImageGenerator` and `EntityImagePanel` so clicking the initial image generation button opens the prompt edit panel pre-filled with the auto-generated initial draft prompt. This allows players to inspect and tweak the prompt before confirming generation. Verified clean compilation and build.

- **2026-09-10** — Nanobanana Model Clean Call (`src/lib/imageGeneration.ts`). Removed deprecated `generateImages` method call and Imagen fallback per Google AI Studio SDK deprecation guidance. Focused model list strictly on `generateContent` with Nanobanana / Gemini Flash Image models (`gemini-3.1-flash-image`, `nanobanana-2`, `gemini-3.1-flash-lite-image`, `gemini-2.5-flash-image`), maintaining 62s cooldown and prompt edit persistence. Verified clean build and linting.

- **2026-09-10** — Nanobanana Model Fallbacks for 404 Resolution (`src/lib/imageGeneration.ts`). Expanded `generateImageBytes` model cascade to include `gemini-3.1-flash-image`, `gemini-3.1-flash-lite-image`, `gemini-2.5-flash-image` (Nanobanana), `gemini-2.0-flash-exp`, and `imagen-3.0` models. This prevents HTTP 404 errors when standard or free-tier Gemini API keys attempt image generation. Verified clean build and linting.

- **2026-09-10** — Nanobanana 2 Exclusive Model, 62s Cooldown & Prompt Persistence (`src/lib/imageGeneration.ts`, `src/lib/imageCooldown.ts`, `src/screens/TaleWeaver.tsx`, `src/screens/Codex.tsx`). Set image generation exclusively to Nanobanana 2 (`gemini-3.1-flash-image` / `nanobanana-2`) and removed all secondary fallback chains. Added a global 62-second retry cooldown persisted in `localStorage` (`useImageCooldown`), complete with a countdown timer badge and disabled button states across `TaleWeaverImageGenerator` and `EntityImagePanel`. Fixed retry prompt persistence so user edits are saved as `lastUsedPrompt` and re-displayed when clicking "Retry" again. Verified clean build and linting.

- **2026-09-09** — Image Generation Fallback Pipeline & Log Noise Reduction (`src/lib/imageGeneration.ts`). Streamlined `generateImageBytes` to try valid Gemini image models (`gemini-2.5-flash-image`, `gemini-3.1-flash-lite-image`), removed non-image model calls (`gemini-2.0-flash`) and Vertex-only `:predict` endpoints that produced `404`s on Google AI Studio keys, and silenced console warnings (`console.info`) so rate-limited (`429`) or unpermissioned (`403`/`401`) keys fail silently and cleanly into Pollinations.ai without polluting the browser debug console. Verified clean compilation and build.

- **2026-09-09** — Imagen REST API `:predict` & Pollinations.ai Free Fallback (`src/lib/imageGeneration.ts`). Resolved `generateImages` SDK mismatch (which was failing on Vertex AI-only methods) by replacing it with Google AI Studio's direct REST `:predict` endpoint (`/v1beta/models/imagen-3.0-fast-generate-001:predict`). Added Pollinations.ai (`https://image.pollinations.ai/prompt/...`) as a guaranteed zero-config, no-auth, keyless fallback when Google API keys hit quota limits (`429`) or Puter returns `401`. Verified clean compilation and build.
- **2026-09-09** — Fast Imagen Models Sequence (`src/lib/imageGeneration.ts`). Updated `generateImageBytes` to prioritize low-latency fast models (`imagen-3.0-fast-generate-001` and `imagen-4.0-fast`) in the Imagen generation sequence, and enhanced `generateImagePuter` to pass the fast model preference (`google/imagen-3.0-fast-generate-001`) with graceful fallback to Puter's default generator. Verified clean compilation and build.
- **2026-09-09** — Editable Image Retry Prompt (`src/screens/TaleWeaver.tsx`, `src/screens/Codex.tsx`). Updated `TaleWeaverImageGenerator` and `EntityImagePanel` so that clicking "Retry Image" / "Retry" toggles an inline prompt editing panel pre-filled with the original prompt. Players can edit or tweak the prompt text and press "Confirm & Weave" to generate a refined image. Included a "Reset to default" button to revert to the initial prompt. Verified clean compilation and build.
- **2026-09-09** — Image Canvas Fitting (`src/screens/Codex.tsx`, `src/screens/TaleWeaver.tsx`). Changed entity artwork display containers from `object-cover` (which zoomed and cropped images) to `object-contain` within a dark backdrop frame (`bg-black/60`), ensuring full-aspect images fit cleanly inside the canvas without zooming or clipping. Verified clean build and type check.

- **2026-09-09** — Image Model Notification Toast (`src/lib/imageGeneration.ts`, `src/lib/entityImages.ts`, `src/screens/TaleWeaver.tsx`, `src/screens/Codex.tsx`). Updated `generateImageBytes` to return a `GenerateImageResult` carrying `modelUsed` (`gemini-3.1-flash-lite-image`, `imagen-3.0-generate-002`, `imagen-3.0-fast-generate-001`, or `Puter.js (Free Fallback)`). Added an emerald notification toast badge in both `TaleWeaverImageGenerator` (Tale Overview) and `EntityImagePanel` (Codex) that displays which AI model produced the artwork upon completion. Verified clean build and compilation.
- **2026-09-09** — Puter.js Zero-Config & `gemini-3.1-flash-lite-image` Default (`src/lib/imageGeneration.ts`). Configured `generateImageBytes` to prioritize `gemini-3.1-flash-lite-image` as the primary image generation endpoint when an API key is present, followed by `imagen-3.0-generate-002` / `imagen-3.0-fast-generate-001`, and seamlessly falling back to Puter.js (`txt2img`) if Google API models fail or lack key permissions. Verified clean compilation.
- **2026-09-09** — Puter.js Zero-Config Image Generation Fallback (`src/lib/imageGeneration.ts`). Integrated Puter.js (`https://js.puter.com/v2/`) as an automatic free fallback generator for entity, region map, location, and character portraits. If Google Imagen 3 returns `403 Permission Denied` (e.g. key missing Imagen access) or if no Imagen key is provided, the pipeline seamlessly loads Puter.js dynamically and generates the illustration via Puter's free `txt2img` service, converting the result into a local WebP blob. Verified clean compilation and build.
- **2026-09-09** — Imagen 3 Multi-Model Fallback & API Permission Fix (`src/lib/imageGeneration.ts`). Replaced experimental `gemini-3.1-flash-image` endpoint with official Imagen 3 models (`imagen-3.0-generate-002` and `imagen-3.0-fast-generate-001`) calling `ai.models.generateImages()` via `@google/genai`. Added automatic fallback sequence to handle permission/quota variations across Google AI Studio key tiers cleanly. Verified clean compilation.
- **2026-09-09** — World & Character Preset Save/Load & 403 Image Generation Error Handling (`src/lib/taleWeaverPresets.ts`, `src/screens/TaleWeaver.tsx`, `src/screens/Codex.tsx`). Added `saveTaleWeaverPreset`, `getTaleWeaverPresets`, and `deleteTaleWeaverPreset` in `taleWeaverPresets.ts` to allow players to save all 7 Tale Weaver phases (World, Protagonist, Regions, Locations, Factions, NPCs, Lore, Arc) as named presets in `localStorage`. Added "Save Draft" and "Load Draft" icon buttons to both the Tale Weaver action bar and the Tale Overview modal header, with interactive Save and Load dialogs. Enhanced error handling in `TaleWeaverImageGenerator` and `EntityImagePanel` to detect `403` / `PERMISSION_DENIED` errors (e.g. key missing Imagen access) and provide clear user instructions on checking API key permissions in Settings. Verified clean build and compilation.
- **2026-09-09** — XML Sanitization & Resilient Parsing for Tale Weaver (`src/lib/xmlHelpers.ts`, `src/lib/taleWeaverParser.ts`). Added `sanitizeXmlForParsing` to convert unescaped ampersands (`&`) in AI response text to valid `&amp;` entities before calling `DOMParser`, preventing `xmlParseEntityRef: no name` XML parsing crashes when generating lore-rich regions/locations (e.g. "Fourth-Wing & Navarre"). Implemented a multi-tier fallback parser `parseXmlWithRegexFallback` in `taleWeaverParser.ts` that repairs malformed attribute quotes and uses regex DOM extraction as a safe fallback if DOMParser fails, ensuring draft content is parsed seamlessly without breaking session flow. Verified with `tsc --noEmit` and build.
- **2026-09-09** — Image Generation & Retry in Tale Overview (`src/screens/TaleWeaver.tsx`, `src/screens/Codex.tsx`, `src/lib/imageGeneration.ts`, `src/lib/taleWeaverParser.ts`, `src/App.tsx`). Added interactive image previews and generation controls (`TaleWeaverImageGenerator`) directly inside the Tale Overview modal for Protagonist, Locations, Cast of Characters (NPCs), and Region Maps before diving in. Extended draft interfaces and `beginInspiredTale` to carry `portraitKey`, `imageKey`, and `mapImageKey` forward into the starting campaign data. Updated error handling in both Tale Overview and Codex (`EntityImagePanel`) to specifically detect quota / rate-limit failures (`429` / `RESOURCE_EXHAUSTED`) and provide clear user messaging that generation can be retried immediately or regenerated later in the Codex after quota resets. Verified with `tsc --noEmit` and `npm run build`.
- **2026-09-09** — Dynamic World-Tone Image Generation Prompts (`src/lib/imageGeneration.ts`, `src/screens/Codex.tsx`). Added `styleDirective(world)` helper to `imageGeneration.ts` that derives an illustration style string from `world.genreTone` and `world.eraTechLevel` (defaulting to `'fantasy'`). Updated `buildLocationImagePrompt`, `buildNpcPortraitPrompt`, and `buildRegionMapPrompt` to accept `world` and splice the tone-specific style directive into location, portrait, and map prompts. Updated all Codex call sites to pass `world`. Verified with `tsc --noEmit`, `npm run build`, and a runtime execution test comparing constructed prompt strings between contrasting world settings ("grimdark war epic" vs "cozy slice-of-life romance").
- **2026-09-09** — Updated Tale Weaving (`TaleWeaver.tsx`) to disable the "Dive in" button and show an informational banner in the Tale Overview modal when all 7 phases are blank. Refined `hasPhaseContent` and `hasAnyContent` to check for trimmed string values across all phases, and added a guard in `App.tsx` (`beginInspiredTale`) to prevent campaign creation from an empty Tale Weaving state. Verified clean build and linter passing.
- **2026-09-09** — Docs-only current-state refresh in `PROJECT_REVISION_NOTES.md` (no code touched). Updated §3 with recently shipped systems (Narrative Events, Death & End Game Rules, NPC Kinship, Tale Weaving & Seeding integration) and added a removal note in §4 for Novel Weaver as a follow-up to the prior archiving pass.
- **2026-09-09** — Updated the image generation pipeline to use `@google/genai` with the `gemini-3.1-flash-image` model per custom AI Studio system instructions, replacing the previous raw `fetch` to `gemini-2.5-flash-image`. Verified mapping for NPCs, Locations, and Regions. Modified TaleWeaver flow to require opening the "Tale Overview" modal before starting the campaign via the "Dive in" button. Verified cleanly with `tsc --noEmit`.
