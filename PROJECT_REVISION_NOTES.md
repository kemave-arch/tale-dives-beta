# Tale Dives — Project Revision Notes

**Read this file first, always.** It carries the current state, the "read this before touching X" warnings, and the pending-work list — everything a session actually needs to resume work. 

For the complete historical log of past decisions and older entries, see [`PROJECT_REVISION_NOTES_ARCHIVE.md`](./PROJECT_REVISION_NOTES_ARCHIVE.md).

## 0. How to resume
- **Start the dev server:** `npm run dev` (runs on `0.0.0.0:3000` per `package.json`).
- **Check the preview:** Open the shared URL or dev URL.
- **Run the checks:** `npm run typecheck` and `npm run build` must pass clean.

## 1. What Tale Dives Is
An immersive, LLM-driven fantasy novel game. The protagonist's actions and the world's responses are generated as narrative prose by Gemini, but long-term memory, game state (inventory, codex, NPCs, factions, quests), and mechanics are tracked deterministically in client-side TypeScript structures and parsed from the LLM's structured `<sync>` output each turn. It runs purely client-side (React + Vite) with no backend, storing state in `localStorage` with optional Google Drive backup.

## 2. Current File Inventory
- `src/App.tsx`: Main router, global state (`game`, `campaigns`), turn loop manager (`sendAction`).
- `src/screens/`: Major UI views (`Title`, `MainMenu`, `Chronicle`, `Codex`, `TaleWeaver`, etc.).
- `src/components/`: Sub-components (`glassChrome.tsx`, `seedweaver/`).
- `src/lib/`: Core logic (`taleWeaving.ts`, `taleWeaverParser.ts`, `store.ts`, `jitContext.ts`, `xmlTurnParser.ts`, `seeding.ts`, `npcs.ts`, `quests.ts`, `imageGeneration.ts`, etc.).
- `src/types.ts`: Master types (Campaign, TurnResponse, Codex entries).
- `src/api/`: Gemini API integration and turn/seeding contracts (`providers/gemini.ts`, `xmlTurnContract.ts`, `taleWeaverContract.ts`).
- `src/data/`: Static data (`classes.ts`, `starterTemplates.ts`, `soundtrackManifest.ts`).
- `PROJECT_REVISION_NOTES_ARCHIVE.md`: The full historical log.

## 3. What's Built (Current State)
- **Tale Weaving (Inspired Mode):** A phased, generative setup flow (World → Protagonist → Cast → Lore → Arc) using the LLM to draft a rich starting state. Includes safety confirmations on generation. Tale Weaving's Arc phase also captures Narrative Events and Death/End Game Rules interactively.
- **Quick Play:** The 3rd Story Mode option (`screens/QuickPlay.tsx`) — 3 plain questions (world/inspiration, protagonist, dive-in/relationships) instead of Tale Weaving's full 7 phases, but internally it's the exact same generator (`runTaleWeaverPhase`/`mergeTaleWeaverDraft`), just run in the background between screens rather than shown live. Step 4 is the same Narrative Settings card the Arc phase uses (`NarrativeSettingsCard`, extracted from `TaleWeaver.tsx` and shared); step 5 is the **Tale Initiation Overview** — an accordion review of everything generated, maintaining the same seamless light vellum reading surface (`parchment-surface !bg-[#fbf8f3]`) and unified button row (`Back` / `Dive In`) as the rest of the flow to eliminate any jarring theme jumps. Dive In routes through `DiveLoadingScreen` (male/female diving art) before landing on Chronicle, same as Original Mode did. Draft autosaves silently to `localStorage` (`lib/taleWeaverAutosave.ts`, `TaleWeaverAutosaveMode = 'full' | 'quickplay'`) so an accidental exit doesn't lose typed answers — this also now covers the full 7-phase Tale Weaver, not just Quick Play.
- **Original Mode retired:** removed from Story Mode Selection (`StoryMode.tsx` now offers only Inspired Mode and Quick Play) now that both cover its use cases robustly. Its backing code (`screens/TaleDiveWeaver.tsx`, `lib/seeding.ts`, `worldSeedContract.ts`/`worldSeedParser.ts`) is unreferenced from `App.tsx`'s router but was left in place rather than deleted outright — flagging here so a future pass doesn't mistake it for dead/unused-by-design and can decide separately whether to remove it.
- **Narrative Events:** `Campaign.narrativeEvents?: Dict<NarrativeEvent>` — condition-triggered reactive story complications (distinct from the linear `Campaign.beats[]` spine), checked client-side each turn via `lib/narrativeEvents.ts` against the same trigger vocabulary Codex Discovery uses (`flag`/`location_visit`/`npc_met`/`quest_complete`/`manual`), plus a `story` trigger where the model itself emits `<event_trip id="..." />` when a freeform prose condition is met.
- **Death Rules & End Game Rules:** `Campaign.deathRule?: 'soft_fail' | 'permadeath'` (plus `deathInstructions`) branches defeat handling between the existing recovery beat and a real story-ending death; `Campaign.endGameRules?: Partial<Record<EndingOutcome, string>>` is per-outcome narration guidance surfaced to the model on the existing `!conclude`/final-beat ending triggers.
- **NPC Kinship:** `KinshipType` (`'parent'`/`'sibling'`/`'child'`/`'spouse'`/`'mentor'`/`'clan'`) on `NpcEntry.kinship`, hard-gating INTIMACY-track escalation for family relations regardless of Trust/Affection tier.
- **XML Turn Engine:** The narrator emits a `<nar>` block for prose and a `<sync>` block (parsed by `xmlTurnParser.ts`) containing exact state updates (`<loc>`, `<npc>`, `<item>`, `<enrich>`, `<project>`, `<breakthrough>`, etc.).
- **JIT Context:** Slices the massive Codex into a small ~40-80 token header (`jitContext.ts`) injected into the prompt each turn, based on who is present or relevant.
- **Codex & Discovery:** Tracks Lore, NPCs, Factions, Locations, Items, Quests, Skills, Projects, and Bestiary. Items are gated by `DiscoveryState` (Known vs. Hidden).
- **Client-Side Image Generation:** Generates UI assets (character portraits, realm art) on the fly via Gemini (`lib/imageGeneration.ts`), storing them in IndexedDB.
- **Lore Accuracy System:** Optional, Tale-Weaving-only "Novel Inspiration / Author / Canon Scope Boundary" fields (`WorldData.sourceScope` gates this — a bare `sourceTitle` stays attribution-only, unaffected). When set, injects a lore-accuracy + spoiler-boundary contract into Tale Weaving's phase calls and every turn's JIT context (`jitContext.ts`), and resolves canon-accurate physical/environmental descriptions for NPC portraits and Location images (`lib/canonDescription.ts` — NPCs via a fixed 10-slot template, Locations as one freeform paragraph). Uses **direct references**: the entity's real name and a direct citation of the source title/author/scope both reach the image model (`imageGeneration.ts`'s `canonReferenceLine`, appended to all three `buildXPrompt` functions whenever `sourceTitle`+`sourceScope` are set) — live testing against `gemini-3.1-flash-lite-image` confirmed direct references to real novels/characters aren't refused, and a name-redacted description had produced portraits far from actual canon (an earlier iteration of this system redacted names for IP-caution reasons; reversed after that test). Continuity across regenerations is preserved via `NpcEntry.canonAppearance`/`LocationEntry.canonDescription`. Region maps get the same direct-citation treatment automatically (no separate wiring needed — `buildRegionMapPrompt` reads the same `world` object). A dev-only **Image Prompt Lab** (Settings → AI Model → Open Image Prompt Lab) drives the real prompt/resolution functions directly for iterating on wording.
- **Background Music:** Context-aware soundtrack crossfading based on turn states.
- **Turn-Time Region/Location/Area Logic:** `Player.areaId?: string` tracks which of the current `LocationEntry.areas` (if any) the player is in, cleared automatically whenever `locId` itself changes. `<turn>`'s `area`/`areadisp`/`areadesc` attributes (xmlTurnContract.ts) let the model move the player between a location's own pre-authored areas, discover a genuinely new one, or step back to the location's general space (`area="none"`) — surfaced every turn via `jitContext.ts`'s "Areas within X"/"Current Area" lines, 0 tokens for the common case of a location with none. `<turn>`'s `region`/`regiondisp`/`mapx`/`mapy` let a genuinely NEW location introduced mid-play (not just at Tale creation) get a real region assignment and 0-100 map coordinates instead of always defaulting to `region: 'Unmapped'` (`lib/locations.ts`'s `ensureLocation`) — the model reasons about placement using "Known Regions"/each known location's own region+xy (also newly surfaced in `jitContext.ts`), grounded in the Tale's own real-world source geography when a Lore Accuracy Contract is active, or Earth-like region/country proportions otherwise. `App.tsx`'s turn loop self-heals a live-caught model confusion (reaching for a brand-new `loc_id` instead of `area=""` when moving into one of the current location's own areas) by redirecting through effective_* values rather than the raw turn fields, and `lib/codex.ts`'s `{{Term|loc}}` auto-registration dedup (`isKnownAreaName`) guards the same fork one level down for inline keyword tags.
- **Data Persistence:** Client-side `localStorage` with multiple campaign slots and optional Google Drive backup/restore.
- **Narrative Settings:** Per-campaign preference dials, same pattern as Prose Depth — `Campaign.pov?: 'first' | 'third'`, `Campaign.narrationMode?: 'reactive' | 'immersive'` (Reactive narrates only the world's reaction to the player's own words; Immersive polishes the player's raw input into their character's own words/thoughts/actions as one continuous scene), `Campaign.difficulty?: TaleDifficultyConfig` (`TALE_DIFFICULTIES` in `turnContract.ts` — Chill/Easy/Balanced/Hard/Extreme, each with its own outcome-leaning guidance). All three are threaded into the prompt every turn via `jitContext.ts`, editable per-campaign in Settings → Gameplay, and set once as a final step in Tale Weaving's Story Arc phase (defaults there: Third Person, Immersive, Extreme — the last two both explicitly recommended). The `*asterisks*` Player Statement Override (turnContract.ts rule 3a) is broadened accordingly to also honor narrative/world-steering claims, not just mechanical stat claims. Prose Depth's `IMMERSIVE` tier is renamed `EXPANSIVE` (same values) since "Immersive" now names the Narration Mode setting instead.
- **Tale Weaving Protagonist Class + Starting Skills:** The Protagonist phase's `<protagonist>` tag now also drafts a `class` (a free-text archetype resolved leniently via the existing `getClassById`, same lenient by-name match used everywhere else) plus 0-3 `<skill>` tags (reusing the turn-time `skill_learn` vocabulary) for starting abilities — editable in `TaleWeaver.tsx`'s protagonist card, seeded onto the real campaign in `beginInspiredTale`. Closes a real gap where a Tale Weaving-created protagonist always started on a hardcoded generic class with zero abilities regardless of what the conversation established, unlike the older node-graph creation flow (`ProtagonistNodeModal`) which already had a real Starting Skills editor.
- **Chapter Summary, incremental redesign:** Replaced the old "cold, one-shot multi-paragraph recap" mechanism (a separate LLM call reconstructing a whole chapter's pacing after the fact at the `CHAPTER_TURN_INTERVAL` boundary — the same mechanism behind an earlier "temporal hallucination" bug) with per-turn, per-event grounding at zero extra per-turn cost. `<turn>`'s new optional `chapbeat` attribute (xmlTurnContract.ts) carries one short past-tense sentence, only on a turn that genuinely advances the story; each one is paired with that turn's own real `GameTime` into a `ChapterBeat` and appended to `Campaign.currentChapterLog` (App.tsx). At the chapter boundary the accumulated list freezes onto a synthetic `LogEntry.chapterBeats` and `currentChapterLog` resets to `[]` — no LLM round trip needed for the timeline itself, so `recapChapter`/`chapterStartTime` were removed from `App.tsx`. Presented as a vertical timeline (timestamped dots down a rail): Chronicle's own chapter-milestone card renders a closed chapter's `chapterBeats` this way (falling back to the old italic `chapterSummary` quote for a save that predates this change), and Codex's Chapters category leads with a live "Current Chapter" card showing `currentChapterLog` building in real time plus a **Pending** section (the one active Story Beat, any `advanced` quests, any triggered-but-unresolved Narrative Events) before listing archived chapters the same timeline way. `jitContext.ts`'s "Story So Far" digest now joins a closed chapter's own `chapterBeats` text directly instead of re-deriving it from a stored prose summary. A single follow-up call (`App.tsx`'s `narrateChapter`, reusing the `runSummary` provider method but fed a synthetic history built from the chapter's own frozen beats rather than raw turn history) then weaves those same accurate facts into flowing multi-paragraph prose as `LogEntry.chapterNarrative` — best of both worlds: the timeline stays the accurate, zero-cost source of truth, while a chapter still closes with a literary "previously on..." passage once that one cheap call resolves (rendered under the timeline in Chronicle's milestone card). On next resuming a Tale, if a chapter has closed since `Campaign.lastAcknowledgedChapter`, Chronicle shows a one-time "Story So Far" memo popup with that chapter's narrative (falling back to its beats, then the old `chapterSummary`, whichever is available) — dismissing it persists the acknowledgment so it won't reappear until the next chapter closes.
- **Clickable Keyword Fixes:** `[[Item]]`/`[Skill]` narration markup was previously decorative only — `onTapTerm` (opening a Codex info popup) was wired solely onto `{{Term|category}}` keyword-tag spans, so tapping a highlighted item or skill name did nothing; both now carry their own tap handler (`lib/richText.tsx`). Separately, `onTapTerm`'s `'loc'` resolution checked top-level Locations and Regions but never a location's own nested named Areas — tapping an area-tagged mention (e.g. `{{The Mess Hall|loc}}`) silently did nothing; it now falls back to opening the parent location's popup (`Chronicle.tsx`).
- **Prologue framing:** Turn 1 previously rendered its `action` field as if it were the player's own typed input — but that field is actually the raw seeded world/protagonist/identity text, a technical prompt seed, never something the player wrote. `LogEntry.isPrologue?: boolean` now marks Turn 1 specifically; `App.tsx` omits `action` entirely for that entry (so the player-action card never renders for it) and `Chronicle.tsx` gives it its own styled "The Prologue" eyebrow divider instead of a timestamp/turn-ref header. Only the model's own narration (`nar`) ever shows for that turn now.

## 4. What's Not Built / Known Issues
- Full multi-device sync or backend database (intentionally client-side for now).
- Novel Weaver (a second, alternate Tale-creation flow) was built earlier and has since been fully removed — don't reintroduce it or look for `screens/NovelWeaver.tsx`, it no longer exists.

## 5. Explicitly Requested But Not Started
- Action Suggestion Pills (rendering clickable `turn.act` suggestions into the UI).

## 6. Suggested Resumption Order
- Pick up from the user's latest prompt. Always verify changes with a live click-through in the dev server.

## Full revision history
Every dated session entry through 2026-09-12 has been moved to [`PROJECT_REVISION_NOTES_ARCHIVE.md`](./PROJECT_REVISION_NOTES_ARCHIVE.md). That file is a verbatim continuation of the same log; nothing was edited or condensed, only relocated. Read it only when a specific past decision needs more detail than the summary sections above give — for resuming work, everything above this line is what actually matters.

### New entries below, most recent first.

- **Last updated:** 2026-09-17 — Unified Quick Play Step 5 background to Parchment consistency (`src/screens/QuickPlay.tsx`, `PROJECT_REVISION_NOTES.md`):
  - **Root cause:** In Quick Play, Steps 1–4 used the light vellum reading surface (`parchment-surface !bg-[#fbf8f3]`), while Step 5 (Tale Initiation Overview) previously dropped the parchment surface in favor of a gender-matched wallpaper background (`DiveWallpaperLayer`) under a dark scrim (`bg-black/40`), creating an abrupt, near-pitch-black screen jump at the final step with low-contrast navigation buttons.
  - **Fix (Option 1):** Removed the wallpaper layer and applied `parchment-surface !bg-[#fbf8f3]` consistently across all 5 steps of Quick Play. Replaced the dark-glass buttons (`GlassCTAButton` and amber-on-dark Back link) in `TaleInitiationOverview` with the unified, high-contrast vellum button row (`Back` / `Dive In` with `Zap` icon) matching the rest of the flow.
  - **Verified:** `tsc --noEmit` and production build clean; dev server running smoothly on port 3000.

- **Last updated:** 2026-09-16 — Quick Play "Source Accurate" checkbox + strengthened Lore Accuracy Contract (`src/screens/QuickPlay.tsx`, `src/lib/taleWeaverAutosave.ts`, `src/api/taleWeaverContract.ts`):
  - Quick Play had no way to opt into the Lore Accuracy Contract the full 7-phase Tale Weaver already exposes via 3 dedicated fields (Title/Author/Canon Scope) on its World Foundation phase — adding those same 3 fields would work against Quick Play's whole "3 plain questions" design. Instead: a single **Source Accurate** checkbox, shown above the prompt on all 3 question screens, persisted across them and through autosave (`TaleWeaverAutosaveState.sourceAccurate`). Checking it derives `world.sourceTitle` from Question I's own answer (already "name a novel, film, or era") the moment any question is submitted — `runTaleWeaverPhase` already reads `accumulated.world?.sourceTitle` for every phase, not just World Foundation, so this one field is all that's needed to thread the contract into protagonist/regions/factions/npcs/lore/arc too. Toggling on after Question I still backfills from the stored answer; toggling off clears it.
  - Strengthened the Lore Accuracy Contract itself (`buildLoreAccuracyContract`): added an explicit instruction to ground locations/NPCs/factions/beats in the source's own real chapter/book/season chronology rather than a generic arc shape, and replaced the old no-scope fallback ("treat only broad, widely-known public facts as safe") with a sharper default — for a multi-part source, the first standalone entry is fair game with full depth and accuracy (not just "broad facts"); a single self-contained work is fair game in full. This is what makes "correct locations/NPCs/order, but don't spoil book 2 of a trilogy" actually possible with no scope typed in — the old fallback was too shallow for exactly the case the new checkbox relies on by default.
  - Verified live: checking the box and answering Question I with "Fourth Wing by Rebecca Yarros, staying within Book 1 only" produced `world.name: "Navarre"` with real Basgiath/Poromiel details, and real Book 1 locations/factions (The Parapet, The Riders Quadrant, Basgiath War College Grounds) instead of generic invented ones.
  - Also confirmed, no code change needed: each of the 7 `TALE_WEAVER_PHASES` already fires as its own separate API call in sequence (`queueStages`'s for-loop awaits `runTaleWeaverPhase` per key) — world/regions/factions under Question I are 3 distinct calls today, not one combined prompt.

- **Last updated:** 2026-09-16 — Fixed a fully-narrated turn getting discarded when the model omits `<sync>` entirely (`src/types.ts`, `src/api/providers/gemini.ts`, `src/App.tsx`):
  - A player-submitted debug log surfaced a live case: a Prologue call wrote a complete, finished scene (`finishReason: STOP`, not a `MAX_TOKENS` cutoff — confirming this wasn't a token-ceiling truncation) and simply never emitted a `<sync>` block at all. `parseXmlTurnResponse` correctly threw (`"No <sync> block found"`), but `App.tsx`'s fallback branch discarded the real, fully readable narration behind a `"(Repairing State) ..."` placeholder — even though `gemini.ts`'s own Stage 3 Fallback Reader comment already documented the intent that this exact case should "extract pure prose and render it directly."
  - Added `RunTurnResult.narRecovered` (set in `gemini.ts` based on whether `extractXmlNarrative` found a real `<nar>` block vs. only the last-resort raw blob) so `App.tsx` can tell "real prose was recovered, `<sync>` just wasn't there" apart from "nothing coherent came back at all." When true, the turn is now treated as the genuine narrated turn it is: `nar` renders unprefixed, `turnCount`/`turnRef` advance normally (state/time/location simply carry forward unchanged, since no mechanical deltas were readable), and the same `{{Term|category}}` keyword-link registration pass every other turn gets still runs off the recovered `nar` text alone (no parsed `<sync>` needed) — so NPCs/locations the scene actually mentions still get a Codex stub instead of vanishing along with the rest of the turn's bookkeeping. The true "nothing readable at all" case still falls back to the repair placeholder, unchanged.
  - Also confirms the token ceiling was *not* reduced/removed for this call — the opposite: `App.tsx`'s `isWorldSeedingTurn` branch deliberately gives the Prologue/Turn-1 call the API's own output ceiling (`MAX_OUTPUT_TOKENS_CEILING`, 65536) rather than the campaign's Prose-Depth-based cap (max 6144), specifically so it's never cut off mid-scene. That's pre-existing, documented behavior (`turnContract.ts`'s comment above `MAX_OUTPUT_TOKENS_CEILING`), not something toggled this session — the missing `<sync>` here was the model choosing to stop after a "finished-feeling" scene, unrelated to budget.
  - Verified by reproducing the exact failure against the real regex/parser logic (`parseXmlBlock` throws on the literal missing `<sync>`, `extractXmlNarrative` recovers the real prose) rather than guessing; `tsc --noEmit` and `npm run build` both clean.

- **Last updated:** 2026-09-15 — Quick Play mode, Prologue framing, Original Mode retirement, Tale Weaver autosave, and 6 live-QC-caught turn-parsing/dedup bugs (`src/screens/QuickPlay.tsx`, `src/lib/taleWeaverAutosave.ts`, `src/lib/taleWeaving.ts`, `src/screens/TaleWeaver.tsx`, `src/screens/StoryMode.tsx`, `src/App.tsx`, `src/screens/Chronicle.tsx`, `src/types.ts`, `src/api/taleWeaverContract.ts`, `src/lib/taleWeaverParser.ts`, `src/lib/xmlHelpers.ts`, `src/lib/xmlTurnParser.ts`, `src/lib/codex.ts`):
  - **Quick Play** (see §3 above for the full shape): new 3rd Story Mode option, 3 questions driving the exact same 7-phase Tale Weaver generator in the background, a shared Narrative Settings step, and a Tale Initiation Overview finale reusing Original Mode's wallpaper/Dive In/DiveLoadingScreen treatment.
  - **`lib/taleWeaverAutosave.ts`** (new): silent `localStorage` draft autosave, distinct from the named Save Preset library, covering both Quick Play and the full 7-phase Tale Weaver — an accidental exit no longer loses typed answers.
  - **`lib/taleWeaving.ts`'s `mergeTaleWeaverDraft`** (new, shared): extracted the phase-merge switch (world/protagonist whole-entity replace; regions/locations/factions/npcs/lore/beats/narrativeEvents accumulate-with-dedup-by-id) out of `TaleWeaver.tsx` so Quick Play and the full flow can't drift out of sync.
  - **Prologue framing** (see §3 above): Turn 1's raw seed dump no longer renders as if player-typed.
  - **Original Mode retired from Story Mode Selection** (see §3 above) — kept as unreferenced code for now rather than deleted.
  - **Live QC (Playwright + real Gemini API, up to ~19 turns per pass, several full passes) caught and fixed 6 real data-loss/dedup bugs, each confirmed by re-running the actual production parser/registration function directly against the real failing payload before and after the fix:**
    1. `lib/xmlHelpers.ts`'s `signToDelta` threw on a bare `="` trust/affection delta (meant "no change") instead of treating it as absent — cost ~30% of turns in the first full pass (each throw discarded that entire turn's real narration via the "(Repairing State)" fallback, since a parse failure never increments `Campaign.turnCount`). Now returns `undefined` for `"="` (and warns+returns `undefined`, rather than throwing, for any other unrecognized value).
    2. `lib/xmlHelpers.ts`'s `optTierWord` threw on `party="none"` instead of treating "none" as absent, same failure class. Now treats a `"none"` value (any case/whitespace) as not-set before falling through to the strict `reqTierWord` check.
    3. `lib/xmlTurnParser.ts` hard-required at least one `<act>` suggestion-chip tag — a real, otherwise-valid turn (real narration, real `<sync>`) that simply omitted suggestion chips was discarded entirely. `act` is optional downstream (`LogEntry.act?: string[]`); the length check is removed.
    4. **Location-id resurfacing:** after several turns correctly using a location's real id, the model spontaneously reverted to the literal seed placeholder `loc_start` (present in Turn 1/Prologue's own context) with no `loc_disp`, creating a duplicate Locations entry for the same place. `App.tsx`'s turn-processing block adds a `staleIdNoRename` check (new `loc_id` unknown, no `loc_disp` given, current location entry exists → treat as "no real move," keep the existing `Player.locId`) alongside the pre-existing `misfiledAsLoc` area-move check.
    5. **Keyword-link stub duplication:** `lib/codex.ts`'s `applyKeywordLinks` guarded `{{Term|loc}}`/`{{Term|npc}}`/`{{Term|faction}}` mentions with `isKnownByName()` before auto-stubbing a new Codex entry, but `lore`/`quest`/`beast`/`skill` had no such guard — a narration mention of an already-generated Tale Weaver Lore entry (same display name, different id/slug) created a second `autoLogged: true, category: 'Unknown'` duplicate stub instead of matching the existing real entry. All four categories now get the same `isKnownByName()` guard.
    6. **Stale-placeholder location dedup, missed variant:** bug 4's `staleIdNoRename` self-heal only caught the model reverting to the `loc_start` seed placeholder when it gave no `loc_disp`. A 4th confirmation pass caught the other variant live: reverting to `loc_start` while restating the CURRENT location's own real name as `loc_disp` (e.g. `loc="loc_start" loc_disp="The Threshing Grounds"`, well after `"the_threshing_grounds"` was already registered) — `staleIdNoRename` doesn't fire when a `loc_disp` is present, so this still forked a duplicate Location keyed under the stale id. `App.tsx`'s turn-processing block now also resolves an unrecognized `loc_id` whose given `loc_disp` matches an already-known location's name to that location's real id instead.
  - QC scope covered: dedups (NPCs/Locations/Factions/Lore/Regions/Quests/Bestiary/Skills), blank required fields, fog-of-war/Discovery states, chapter beats/Story So Far digest, location region/map-coordinate completeness, faction/NPC states, and keyword-link click-through — all structurally verified clean against real live-API playthrough dumps after the fixes above (confirmation history: pass 1 caught bugs 1-2, pass 2 caught bug 3, pass 3 achieved 100% turn success and caught bug 4, a structural check on that same pass's dump caught bug 5, a 4th ~17-turn pass with bug 5's fix live caught bug 6, and a 5th pass with bug 6's fix confirmed all 8 Codex categories duplicate-free — that 5th pass's lower turn count and console noise were Gemini 429 rate-limiting from 5 consecutive live-API passes in one session, not an app defect; the existing repair-state fallback degraded gracefully as designed).

- **Last updated:** 2026-09-14 — Off-Hand Equip Slot (`src/types.ts`, `src/lib/inventory.ts`, `src/lib/jitContext.ts`, `src/lib/bangCommands.ts`, `src/App.tsx`, `src/screens/Codex.tsx`, `src/screens/Chronicle.tsx`):
  - **Added a second weapon slot** — `EquipSlot` gains `'offhand'` alongside `'weapon'`/`'armor'`/`'accessory'`, so a `weapon`-type item can now go in either slot (dual-wielding, or a weapon paired with a shield) instead of there being only one weapon slot total. `EQUIP_SLOT_ITEM_TYPE` (`types.ts`) is the new slot → required-item-type map `equipItem` (`lib/inventory.ts`) validates against, since Weapon/Off-Hand are no longer 1:1 with `ItemType` the way Armor/Accessory still are.
  - **Codex's item-detail Equip control** now offers "Equip as Weapon" / "Equip as Off-Hand" side by side for a weapon-type item instead of one ambiguous "Equip" button; Unequip now names which slot it's clearing. `onEquipItem` took an optional `slot?: EquipSlot` param to support this (armor/accessory items are unaffected — still one unambiguous slot, no UI change for them).
  - **`!equip`/`!unequip` bang commands** extended to match: `!equip <item> offhand` (or `secondary`/`off-hand`) picks the off-hand slot explicitly, and `!unequip` now also accepts `offhand`/`secondary`/`off-hand` as a target alongside the existing `weapon`/`armor`/`accessory`. A new shared `equipSlotAlias()` helper (`bangCommands.ts`) resolves any of these spellings to the real `EquipSlot` key for both the bang-command parser and (implicitly) documents the accepted vocabulary in the `!equip`/`!unequip` autocomplete usage strings.
  - **`jitContext.ts`'s always-on `Equipped:` line** now labels each part by slot (`Weapon: ... | Off-Hand: ... | Armor: ... | Accessory: ...`) instead of bare names — necessary now that two equipped items can share the same underlying item type and would otherwise be indistinguishable to the model.
  - **Chronicle's desktop sidebar** gained a fourth "Off-Hand" gear card between Weapon and Armor, same treatment as the existing three.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright pass: equipped an Iron Sword as Weapon and a Steel Dagger as Off-Hand via Codex's new two-button control, confirmed both persisted correctly (`player.equipped: {weapon: "iron_sword", offhand: "steel_dagger"}`), confirmed the Unequip button read the correct slot name, and confirmed Chronicle's desktop sidebar showed all four gear rows (Weapon/Off-Hand/Armor/Accessory) with the right items in each.

- **Last updated:** 2026-09-14 — Tap-to-Inspect Lightbox, Chapter Recap Time-Stamp Fix (`src/screens/Chronicle.tsx`, `src/api/providers/gemini.ts`, `src/App.tsx`):
  - **Added a tap-to-inspect lightbox** to Chronicle: the hero location banner and any Codex-popup entity image (item/NPC/location art) now carry a small "Inspect" badge; tapping either opens a full-screen enlarged view (dark scrim, `object-contain` image, caption, close button) — the one mockup idea from the earlier reskin pass that had been deliberately deferred as an added interactive feature rather than a pure reskin. Verified via pixel-sampling the rendered screenshot (not just visual inspection, which initially misread a correctly-rendered dark overlay as not covering the header) that the scrim genuinely spans the full viewport above every other layer.
  - **Fixed the chapter-recap essay stating explicit clock times/day numbers repeatedly** ("Day 1, 08:00...", "by 09:10...") instead of reading like ordinary prose — a live session log surfaced it. Root cause: the anti-inflation guardrail in `gemini.ts`'s `runSummary` prompt (added earlier specifically to stop the recap exaggerating a short span into "days of hardship") handed the model the *exact* literal start/end timestamp as its proof of honesty, and the model duly restated that timestamp on nearly every beat instead of just keeping pacing honest. Fixed by handing the model only a coarse qualitative span ("a single day" / "about N days" — day-count only, since `GameTime.h` is freeform model-written text and not reliably parseable into real elapsed hours) and telling it explicitly to convey pacing through scene transitions, never explicit clock/day stamps. `App.tsx`'s `narrateChapter` also stopped prefixing every single beat line with its own literal "Day D H:" label (the likely source the model was echoing) — a day marker now appears only when the day actually changes across the list.
  - **Investigated a "repair state" concern** raised against a separately-supplied session log: no `(Repairing State)` fallback text (the real, existing parse-failure recovery path in `App.tsx`) appears anywhere in that log — every turn shown parsed cleanly (`finishReason: STOP`). One turn's pasted response appeared to have no `<sync>` block, but this is far more likely an artifact of manual copy/paste truncation than a real parse gap, since the parser requires a `<turn>` tag (inside `<sync>`) and throws otherwise, which would have triggered the exact fallback in question. No code change made here — flagged for the user to point at a live in-app instance if it recurs.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright pass (hand-seeded `localStorage` campaign + seeded IndexedDB image blobs for the hero location/item/NPC portrait, since no LLM API key or generated art exists in this sandbox): confirmed the hero banner and NPC portrait render with real images, the "Inspect" badge opens the lightbox, and pixel-sampled the resulting screenshot to confirm the scrim truly covers the full viewport rather than trusting a visual read that had been wrong.

- **Last updated:** 2026-09-14 — End of Turn Recap Accordion + ChroniclerDesign.md (`src/screens/Chronicle.tsx`, `ChroniclerDesign.md`):
  - **Added `TurnRecapAccordion`** — a collapsed-by-default "— End of Turn N · Recap ▾ —" toggle folding every mechanical delta a turn produced (level-ups, class evolutions, Codex discoveries, Narrative Events, crafting-ready notices, minion losses) behind one tap, replacing the always-visible pill-badge soup those used to render as. Renders nothing at all (no toggle) when a turn produced zero such facts — never an empty affordance.
  - **Deliberately not** the reference mockup's fixed 2-column grid of four hardcoded categories (which would need filler content on a turn that didn't produce four kinds of news, two of which were flavor specific to the mockup's own Fourth Wing example anyway) — built instead as a single-column dossier list, one row per kind of delta, built only from what the turn's own `LogEntry` actually carries. `entry.ending` (the Tale Concludes banner) stays outside the accordion, always visible, since an ending shouldn't be one tap from invisible.
  - **`ChroniclerDesign.md`** (new file, root) records the Chronicle-only "Editorial Vellum" design system from the prior entry below — palette, typography scoping mechanism (`.parchment-surface` now also carries the Literata font override), layout (the Bento-style desktop sidebar, since the reference mockup has no desktop mode of its own), each component's real-data mapping, and — importantly — what was deliberately left out (image lightbox, bottom tab bar, font-size scaler, NPC dialogue cards) so a future pass doesn't rediscover the same ground. Scoped explicitly against the pre-existing `DESIGN.md`, which still documents every other screen's dark theme untouched.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright pass (hand-seeded `localStorage` campaign, same method as the prior entry): confirmed the accordion is collapsed by default, expands to show all four seeded delta types (Level Up, Codex Updated ×2 with working clickable Codex links, Narrative Event, Crafting Ready) in the dossier layout, and collapses again on toggle.

- **Last updated:** 2026-09-14 — Chronicle "Editorial Vellum" Visual Overhaul + Context-Slice Header Timestamp (`src/screens/Chronicle.tsx`, `src/index.css`, `index.html`, `src/lib/jitContext.ts`):
  - **Chronicle reskinned from dark obsidian glass to a light "editorial vellum" look**, per a reference mockup/design doc pairing a warm parchment ground (`#faf8f5`/`#fbf8f3`) with `Cinzel` display type, `Literata` reading prose, `Plus Jakarta Sans` UI labels, `JetBrains Mono` timestamps, and ochre/gold accents (`#b08830`/`#8d6b1d`) — scoped entirely to the Chronicle screen (the app's one other screen family, Codex/Settings/creation flow, keeps its existing dark theme; explicitly a first phase, not an app-wide reskin). Every existing feature was kept and only reskinned, never dropped: the desktop left sidebar (character/equipment/vitals), turn navigator, Codex-entry popup card, Story So Far memo, bang/slash command menus, the quick-access Codex drawer, Edit/Retry/Delete controls, and the debug payload tools all still work, just repainted.
  - **New real-content additions**, not just recolors: a full-bleed hero banner (the current location's generated art, when one exists, with a gradient fade into the vellum ground and a location/day badge overlay) replaces the old "tint the whole scroll background" trick; a "Turn N · Chapter M" eyebrow line is parsed from each entry's own `turnRef` instead of the old plain timestamp; an illuminated drop-cap now opens the first narrated turn of each chapter (computed from real chapter-boundary entries, not applied to every turn); present-NPC portraits became small named medallions in the reading flow instead of tiny floating avatar chips — still just the character's photo next to the narration, no fabricated per-line dialogue attribution/quote cards, since the turn data has no such field.
  - **Deliberately scoped out** per direct feedback mid-build: no glassmorphism/backdrop-blur anywhere in the new surfaces (flat, lightweight panels + hairline borders instead — `backdrop-blur` no longer appears anywhere in `Chronicle.tsx`); no NPC dialogue "voice plate" cards; the mockup's own bottom tab bar, font-size scaler, and tap-to-inspect lightbox were left out as out-of-scope UI additions ("buttons and features we can adjust later"); the mockup's roleplay-flavored system copy ("FATE THREAD FALTERED", "The thread of fate is being woven...") was replaced with plain, universally-understood labels ("Request Failed", "Generating...") per direct feedback against in-fiction language for system chrome.
  - **`ApiErrorPanel` intentionally left on its own fixed dark palette** (pre-existing design decision, documented in its own comment) since it's meant to stay legible regardless of which skin the screen around it is wearing.
  - **`jitContext.ts`**: the per-turn `[ACTIVE CONTEXT SLICE]` header now stamps the in-world day/time directly on the header line itself (`[ACTIVE CONTEXT SLICE — Day 18, Morning Muster]`) rather than only two lines further down — the model should be able to orient temporally from the very first line of every turn's context, not have to scan for it. It's the only such bracketed context-block header anywhere in the codebase.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright pass against the dev server (a hand-seeded `localStorage` campaign, since no LLM API key is configured in this environment) at both mobile (420×860) and desktop (1280×860) viewports: confirmed the hero banner/eyebrow/drop-cap/action-card/badges/suggested-actions render correctly, the desktop Bento-style sidebar reads as a coherent set of light cards, and the Codex-entry popup card opens in the new light palette with working attribute badges and the "Open in Codex" action.

- **Last updated:** 2026-09-14 — Tale Weaver Badge Cleanup, Player Action Card in Chronicle (`src/screens/TaleWeaver.tsx`, `src/screens/Chronicle.tsx`):
  - **Removed the "Priority Anchor" badge** from Phase 1's Novel Inspiration card — a static label carrying no information (it never changed, never linked to anything), per direct feedback that it added no useful context.
  - **Live-audited all 7 Tale Weaver phases** (screenshots of each phase's empty state) looking for further cleanup opportunities — found the phases already consistent with each other (same empty-state card pattern, same list-header/edit-form treatment across World/Protagonist/Regions/Factions/Cast/Lore/Arc), so no further changes were made there rather than manufacturing busywork.
  - **Beautified the player's action display in Chronicle**: it previously rendered as plain italic gold text flush against the timestamp above it, forced onto the same line as the turn-state badge (which wrapped awkwardly for a short action next to a long badge). Now wrapped in its own soft card (light gold-tinted background, rounded corners, a small feather icon) that reads as a distinct "your voice" beat separate from the narration beneath it, with the turn-state/mood badges moved to their own row below instead of crammed inline.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live-verified via Playwright screenshots — before/after comparison of the player-action card, and full-phase screenshots confirming the Tale Weaver badge removal and phase-to-phase consistency.

- **Last updated:** 2026-09-14 — Explicit Blank-Line Paragraph Breaks, New-Turn Scroll-to-Top (`src/api/turnContract.ts`, `src/screens/Chronicle.tsx`):
  - **What changed**: two small UX-craft fixes from direct feedback.
    1. **Rule 1b (Paragraph Breaks)** didn't specify that a break between two narration paragraphs must be a genuine blank line (two newline characters), only that there be "a line break" — ambiguous enough that the model sometimes used a single newline between paragraph-level beats, which renders (under the app's `white-space: pre-wrap` CSS) as a soft continuation rather than a real paragraph gap. Made the double-newline requirement explicit, while clarifying it's specifically about narration-to-narration breaks — rule 1c's own single-newline convention for a run of consecutive thought/dialogue lines is untouched and still correct.
    2. **New-turn auto-scroll landed on the wrong edge**: Chronicle.tsx scrolled to the absolute bottom of the page (`scrollHeight`) every time a new turn arrived, which puts the END of narration the player hasn't read yet on screen — forcing a scroll back up to actually start reading. Changed to scroll to the new turn block's own top edge instead (reusing the same header-offset math the block navigator's `scrollBlockIntoView` already uses), so the view opens on the turn's first line, where reading should start.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live-verified via an injected synthetic campaign (4 turns, each padded to real scroll-worthy height): confirmed the page settles with the newest turn's own timestamp header at the top of the viewport (a measured ~648px gap from the true page bottom, roughly one viewport height, not the ~0px gap the old bottom-scroll produced) and its first paragraph immediately visible, rather than opening already-scrolled past the whole turn.
  - **Not yet resolved**: the same feedback also raised a segmented, tap-to-continue reveal for narration (to avoid a long turn appearing all at once) — a materially bigger UX change (chunked reveal + temporarily disabling the input bar) than either fix above, intentionally not started without confirming scope first.

- **Last updated:** 2026-09-14 — NPC Appearance Anchor, Generalized Show-Not-Tell, Substantiated Intrigue Rule (`src/types.ts`, `src/api/xmlTurnContract.ts`, `src/lib/xmlTurnParser.ts`, `src/lib/npcs.ts`, `src/api/turnContract.ts`):
  - **What changed**, after comparing Tale Dives' narration/NPC-writing against sample output from a different LLM RP engine and identifying three concrete craft gaps worth closing:
    1. **NPC Appearance Anchor**: `NpcEntry.appearance` already existed as a manual-CRUD/seeding-only field with no turn-time channel — the model had no way to set or revise it during play, unlike `personality`/`heldWeapon`/`wornArmor`, which are all restated every turn an NPC is present as a stable anchor. Added `appearance="..."` to `<npc>` (`xmlTurnContract.ts`, parsed in `xmlTurnParser.ts`, applied in `lib/npcs.ts`'s `applyNpcUpdates` — identical discipline to `personality`), and now restated in `describePresentNpc`'s per-turn context line. Closes the "every NPC in the cast defaults to the same generic descriptor" failure mode observed in the comparison engine's output (three different characters all described as having the same eye color).
    2. **Generalized "show it in the body, not the label"**: this discipline already existed but only for the DESPAIR turn-state's own guideline text. Folded it into rule 1d (NPC Behavior) as a general instruction for any NPC reaction in any state — naming an emotion adjective directly ("genuine embarrassment," "a desperate, confused hunger") is now explicitly a last resort, not a default, matching a recurring weak spot found in the comparison output.
    3. **New rule 1i, Substantiated Intrigue**: bans declaring a scheme/trap/plan is succeeding ("the trap was perfectly tuned," "the pieces were all on the board") without that turn's own dialogue or action making it legible — another recurring pattern in the comparison output, where "something clever is happening" got asserted repeatedly with no actual content behind it.
  - **Live QC test** (real API, `gemini-3.5-flash-lite`, via a two-turn negotiation scene run through the app's own real `runTurn`/`parseXmlTurnResponse` — fetched in a Node harness using `NODE_USE_ENV_PROXY=1` to route around this sandbox's browser/proxy TLS limitation, then parsed through a live browser page since `parseXmlTurnResponse` needs `DOMParser`): confirmed all three land cleanly. An NPC's `appearance` was set once on introduction ("Lean build, ash-blonde hair cropped short, jagged scar through the left eyebrow") and restated verbatim on the next turn's `npc_mem_up`, with the prose itself organically reusing the scar as a specific callback on the second turn rather than re-describing her generically. Across both turns, every reaction was physical/behavioral (stillness in the shoulders, a thumb tracing a mug's rim, a scar twitching) with zero directly-named emotion adjectives. The negotiation's actual leverage (a blackmail proposal, a concrete trade of a stolen shipping seal for the ledger's silence) was carried entirely through specific dialogue, never asserted abstractly as "a plan succeeding." Paragraph breaks and thought/dialogue isolation (rules 1b/1c) held throughout, and pacing stayed proportionate turn to turn with no rushed escalation.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live QC test above.

- **Last updated:** 2026-09-14 — "Repairing State" Unclickable-Chip Fix, NPC Presence Lingering, Multi-Turn Pacing Restraint (`src/App.tsx`, `src/api/xmlTurnContract.ts`, `src/api/turnContract.ts`):
  - **Fixed a real unclickable-content bug**: a turn whose XML failed to parse falls back to a `nar: "[Repairing State] ..."` entry, but `[Repairing State]`'s own square brackets are exactly `richText.tsx`'s `[Skill]` markup convention — it rendered as a bold, tappable-looking skill chip that found no such skill and silently no-op'd when tapped, precisely matching a live report of "unclickable stuff... Repairing state." Changed to `(Repairing State)` (parens, which no rich-text convention matches) — verified live that it now renders as plain text with no fake tap affordance.
  - **Root-caused a real "NPCs linger and keep chiming in" gap**: `applyNpcUpdates()` stamps `lastSeenLocId` (what drives whether an NPC counts as "present" for the next turn's context) with the SAME location for every NPC in that turn's `npc_mem_up` list — including one the model only touched to restate an unchanged aff/trust/personality "for continuity" rather than because they're actually still in the scene. Once the scene or location moves on, an NPC who never explicitly left (no `party="departed"`) but also never explicitly followed keeps getting marked present at wherever the player currently is, letting them keep reacting/chiming in long after they should have gone quiet. This is a model-compliance gap, not a client bug — the client already only tracks presence off what the model itself reports — so fixed by hardening `xmlTurnContract.ts`'s `<npc>` "party" rule with an explicit anti-pattern warning: once a scene moves on from an NPC without them joining as a companion, stop touching their record out of habit; an `<npc>` tag is a claim that they're present right now.
  - **Added a Multi-Turn Pacing Restraint rule** (`turnContract.ts` §1h) after a live report of hurried pacing — a completed quest/goal immediately triggering a chapter's worth of major confrontations, secrets, and relationship escalation within the same scene/minutes of in-fiction time. The new rule explicitly tells the model that resolving a goal is not a cue to manufacture the next big complication, that quieter/lower-stakes turns are often correct even several in a row, and to weigh how much story-significant ground has been covered against how much in-fiction time has actually passed before reaching for a Tale's biggest turns.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. The Repairing-State fix live-verified via an injected synthetic campaign entry: confirmed no `<strong>` (skill-styled) span renders for the literal text and the parenthesized wording displays as ordinary prose. The NPC-lingering and pacing fixes are prompt-side hardening (no client code to unit-test against) — flagged for a follow-up live QC pass to confirm the model actually complies with the new wording in practice.

- **Last updated:** 2026-09-14 — Tale Weaving Phase 3 Parent-Child Fix, Item Tap/Auto-Registration Fix, Lore Accuracy Hardening, Soundtrack Reset Fix (`src/screens/TaleWeaver.tsx`, `src/lib/richText.tsx`, `src/lib/keywordLinks.ts`, `src/lib/codex.ts`, `src/App.tsx`, `src/api/taleWeaverContract.ts`):
  - **Fixed Tale Weaving Phase 3's parent-child display gap**: Regions and Locations rendered as two independent flat lists — a Location's owning Region was buried in a small text fragment inside its card, easy to miss, and a Region card gave no indication of what it contained. Refactored to nest each Region's Locations visibly beneath it (with a live location-count badge), plus a dashed "Unassigned Locations" section for any location with no/broken `regionId`. Re-parenting a location via its own edit form still works and now visibly moves its card between groups.
  - **Fixed a real unclickable-keyword bug**: a live debug log showed `[[Scribe Quadrant Archives|loc]]` (the model mixing the `[[Item]]` and `{{Term|category}}` tag conventions) tapping through hardcoded as category `'item'`, finding no matching item, and silently no-op'ing. `richText.tsx` now honors a trailing `|category` suffix when it names a real `KeywordLink` category instead of always assuming `item`.
  - **Fixed a second, deeper unclickable-item bug** the same report surfaced (`[[Poison-Drenched Dagger|item]]`, a well-formed tag): items mentioned only in prose were never auto-registered anywhere — `{{Term|category}}`'s auto-registration path (`applyKeywordLinks`) deliberately excludes `item` by design (real inventory items are meant to arrive via `inv_add`), so a `[[Item]]` mention with no matching `inv_add` that same turn had nothing to tap into, ever. Added `parseItemMentions()` (`keywordLinks.ts`) + `applyItemMentions()` (`codex.ts`): any `[[Item]]` mention not already known by name gets a minimal reference-only Codex stub (`type: 'material'`), mirroring how a `{{Term|loc}}`/`{{Term|npc}}` mention already does — never an inventory grant, since `Campaign.items` is a Codex reference dict decoupled from `Player.inventory`'s own ownership map.
  - **Hardened the Lore Accuracy Contract** (Tale Weaving's canon-fidelity system prompt) after a live report of invented, non-canon names and wrong details for real canon entities in a Fourth Wing-sourced Tale: it now explicitly instructs the model to prefer real named canon entities over inventing new ones when the source has ones that fit, to never invent a name that only sounds like it could be canon (reads as imitation rather than clearly original), and to omit or generalize an uncertain specific detail on a real entity rather than assert a confident but invented one.
  - **Fixed the actual "soundtrack resets to track 1" bug** a user reported on entering the story screen or exiting to Main Menu: root-caused via a `HTMLMediaElement.prototype.play` trace injected into a live Playwright session (not guesswork) to a stray, unconditional `onPlayTrack('TempestDive_ost03.opus')` call inside `App.tsx`'s `resumeCampaign()` — a hardcoded literal track name with no relation to game state, firing on every single campaign resume regardless of what was already playing, directly contradicting the surrounding code's own "soundtrack keeps playing across navigation" design comment. Removed; ambient rotation now continues seamlessly across a resume/exit/resume round trip, confirmed via `currentTime` advancing continuously across the whole sequence with zero extra `play()` calls firing.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean after each fix. Phase 3 refactor live-verified via Playwright (mobile viewport): added two regions and a location, confirmed the location nests under its region with a correct count badge, then confirmed an unassigned location renders in its own dashed section. Soundtrack fix live-verified via a `play()`-call stack trace across a full Resume → Home → Resume round trip, both before (confirming the bug) and after (confirming zero unexpected restarts, `currentTime` monotonically increasing throughout).

- **Last updated:** 2026-09-14 — Chapter Narrative Recap + "Story So Far" Welcome-Back Memo (`src/types.ts`, `src/App.tsx`, `src/screens/Chronicle.tsx`):
  - **What changed:** Follow-up to the same-day Chapter Summary incremental redesign, after discussing the tradeoff directly: the new vertical timeline is more accurate and free, but reads like a quest log rather than the literary "previously on..." prose the old cold recap gave. Added both back without reintroducing the old mechanism's cost or hallucination risk:
    1. `LogEntry.chapterNarrative?: string` — once a chapter's `chapterBeats` freeze at the boundary, `App.tsx`'s new `narrateChapter()` fires a single non-blocking follow-up call (reusing the existing `runSummary` provider method, unchanged) fed a *synthetic* history built from that chapter's own already-accurate beats rather than raw turn history — the model only weaves known-true facts into flowing multi-paragraph prose, never reconstructs pacing cold, so the original "temporal hallucination" risk doesn't reappear. Renders under the timeline in Chronicle's closed-chapter milestone card once it resolves; a missed/failed call costs only flavor (swallowed, same philosophy as the old recap).
    2. `Campaign.lastAcknowledgedChapter?: number` + a one-time "Story So Far" memo popup (Chronicle.tsx): checked once on mount (not on every chapter close, so a chapter closing while already in-session never triggers it) — if the latest closed chapter's number is greater than what's been acknowledged, shows a modal with that chapter's `chapterNarrative` (falling back to its beats, then the old `chapterSummary` for a pre-redesign save). Dismissing persists the acknowledgment so it won't reappear until the next chapter closes — a genuine "welcome back" recap the next time the player resumes the Tale, not just a notification of what they missed.
  - **Verification:** `npx tsc --noEmit` and `npm run build` clean. Live-verified via an injected synthetic campaign (a closed Chapter 1 carrying both `chapterBeats` and a pre-set `chapterNarrative`, no `lastAcknowledgedChapter`): confirmed the popup appears on first load with the narrated prose, dismissing it persists `lastAcknowledgedChapter: 1` to `localStorage`, and a subsequent fresh reload no longer shows it (while the narrative prose itself stays visible under the milestone card's timeline, as intended).

- **Last updated:** 2026-09-14 — Clickable Keyword Fixes, Tale Weaving Skills/Class Gap, Chapter Summary Incremental Redesign (`src/lib/richText.tsx`, `src/screens/Chronicle.tsx`, `src/api/taleWeaverContract.ts`, `src/lib/taleWeaverParser.ts`, `src/lib/taleWeaving.ts`, `src/screens/TaleWeaver.tsx`, `src/App.tsx`, `src/types.ts`, `src/api/xmlTurnContract.ts`, `src/lib/xmlTurnParser.ts`, `src/lib/jitContext.ts`, `src/screens/Codex.tsx`):
  - **Fixed clickable-keyword bug**: `[[Item]]`/`[Skill]` narration spans were decorative only — the Codex-popup tap handler was wired solely onto `{{Term|category}}` keyword-tag spans, never these two — so tapping a highlighted item or skill name did nothing. Both now carry their own `onTapTerm` handler directly. Separately, tapping an area-tagged keyword (e.g. `{{The Mess Hall|loc}}`, from this session's earlier area/region work) silently did nothing since `onTapTerm`'s `'loc'` resolution never checked a location's own nested `areas` — now falls back to opening the parent location's popup.
  - **Fixed a real Tale Weaving gap**: investigated whether there were gaps in Skills handling between Tale Weaving and turn-by-turn play. Turn-by-turn Skills (`skill_learn`, effort/tier, strain-based affordability) were already solid, but the AI chat-based Tale Weaving flow had zero concept of skills or class at all — `beginInspiredTale` always hardcoded `player.classId` to `PRESET_CLASSES[0]` and `skills: {}`, regardless of what the conversation established, unlike the older node-graph creation flow which already had a real Starting Skills editor. Fixed by having the Protagonist phase's own `<protagonist>` tag also draft a `class` (resolved leniently via the existing `getClassById`) plus 0-3 `<skill>` tags, editable in `TaleWeaver.tsx` and seeded onto the real campaign.
  - **Redesigned Chapter Summary** from a cold, one-shot multi-paragraph recap (a separate LLM call reconstructing a whole chapter's pacing after the fact — the same mechanism behind an earlier "temporal hallucination" bug) to incremental, per-turn grounding at zero extra API cost: `<turn>`'s new `chapbeat` attribute carries one short sentence on a narratively significant turn, accumulated turn-by-turn into `Campaign.currentChapterLog` and frozen onto a synthetic `LogEntry.chapterBeats` at the chapter boundary — no LLM round trip needed, so the old `recapChapter`/`chapterStartTime`/`runSummary` call site was removed from `App.tsx` entirely. Presented as a vertical timeline (per the user's own suggestion) in both Chronicle (closed-chapter milestone card) and Codex's Chapters category, which now leads with a live "Current Chapter" card showing the in-progress timeline plus a **Pending** section (active Story Beat, in-progress quests, triggered-but-unresolved Narrative Events).
  - **Bug caught during live verification**: the first Pending-section implementation filtered story beats by `status === 'active' || status === 'pending'`, which on a fully-authored Tale Weaving arc dumped the ENTIRE remaining outline (dozens of not-yet-reached chapter titles) as a spoiler list. Fixed to only include `status === 'active'` (the beat the story has actually turned toward).
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean after each change. Live Playwright click-through of Tale Weaving's Protagonist phase (Class field + Add Skill flow, confirmed saving and rendering correctly). Chapter Summary timeline/Pending UI verified by injecting a synthetic campaign (closed chapter with `chapterBeats`, live `currentChapterLog`, an `advanced` quest, an `active` beat, and an `active` Narrative Event) via localStorage and confirming both Chronicle's and Codex's rendering paths — this is what caught the Pending-section bug above before it shipped.

- **Last updated:** 2026-09-13 — Live QC Pass: Tale Weaving + Turn-by-Turn Narration (`src/api/taleWeaverContract.ts`, `src/lib/taleWeaverParser.ts`, `src/App.tsx`, `src/lib/xmlTurnParser.ts`):
  - **What changed:**
    1. Ran a live, real-API QC pass (Tale Weaving's 7 phases end-to-end, then several turn-by-turn narration turns) using a Fourth Wing/Violet Sorrengail test Tale, via a Node harness that calls the app's own real `taleWeaving.ts`/`gemini.ts`/`xmlTurnParser.ts` functions directly (browser-based E2E testing was blocked in this sandbox by a TLS-fingerprint incompatibility between headless Chromium and the environment's egress proxy — confirmed via the proxy's own status endpoint showing `ws_closed_mid_exchange` on the CONNECT tunnel to `generativelanguage.googleapis.com`, not a certificate-trust issue; Node's own `fetch` with `NODE_USE_ENV_PROXY=1` worked fine, so all live calls in this pass went through that path instead).
    2. **Fixed a real fog-of-war gap**: Tale Weaving's grammar/parser only supported `hidden`/`teaser` on `<lore>` — Locations, Factions, and NPCs were hardcoded to always-known (`discovery: { state: 'known' }`) regardless of what the model drafted, even though the Codex Discovery system and its UI (`Codex.tsx`'s `MaskedDetail`) already fully support hidden entries for every category. Extended `hidden`/`teaser` to `<location>`/`<faction>`/`<npc>` (grammar, parser, and the Campaign-assembly mapping in `App.tsx`, reusing the exact `validateDiscovery` call lore already used) — verified live: the model produced a genuinely hidden faction and a hidden NPC, each with real teaser text, on request.
    3. **Fixed a hardcoded Arc-phase scale limit**: the Story Arc phase's own rule text capped beats/narrative events at "3-6"/"2-3" regardless of what the player asked for, so a request to map a whole novel's chapter count (e.g. Fourth Wing's 39 chapters) into that many beats/events was silently compressed down. Relaxed the rule to explicitly allow scaling into the dozens when source material has a known chapter/arc structure and the player's guidance calls for that granularity — verified live: produced exactly 39 beats and 39 narrative events, one per chapter, in correct chronological/escalating order matching the book's real progression.
    4. **Fixed a quest-status parsing bug found live on the prologue turn**: the model naturally reaches for `<quest stat="active">` for a brand-new quest (borrowing `<beat>`'s vocabulary — active/completed/skipped), but `<quest>`'s own vocabulary is advanced/completed/failed with no word for "just introduced." The parser's strict vocabulary enforcement threw on the unrecognized value, which silently discarded the *entire* `<sync>` block for that turn (not just the quest) via the existing Stage-3 fallback — losing the prologue's NPC introductions, faction rep, and beat activation along with it. Normalized `"active"` → `"advanced"` at parse time (`xmlTurnParser.ts`) and clarified the grammar's own rule text to reduce recurrence at the source.
    5. Everything else checked out well in this pass: Tale Weaving's field-filling was rich and genuinely canon-accurate (not generic filler) across World/Protagonist/Regions/Factions/NPCs/Lore; turn narration quality, paragraph/dialogue-break discipline, and JIT context construction all read correctly; quest/beat updates applied correctly once the parse bug above was fixed; the existing `*asterisks*` Player Statement Override rule (turnContract.ts 3a, currently documented as mechanical-stat-claim-only) generalized gracefully to a narrative claim in live testing (a fabricated hidden letter) — woven in seamlessly with real narrative weight, no fourth-wall break.
    6. Also found (not fixed — flagging only, since only the user can act on it): the live API key the user provided for this pass is already hardcoded, lightly obfuscated, and committed as `DEFAULT_GEMINI_API_KEY` in `src/lib/store.ts` — shipped into the client bundle and already pushed to the repo. Recommend rotating this key and removing the hardcoded fallback.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean after each fix. Live-verified via direct real-API calls (not mocked) against `gemini-3.5-flash` (Tale Weaving phases, most turns) and `gemini-3.5-flash-lite` (remaining turns, after the flash model's free-tier daily quota was exhausted mid-pass). Full transcripts, raw model responses, and parsed structures saved under `public/reports/` (`qc-transcript-raw.json`, `qc-turns-transcript.json`, `qc-final-accumulated.json`, `qc-campaign-after-turn-*.json`) for reference. Browser-based click-through of the Tale Weaving UI itself and the full 10-turn sample were not completed in this pass (proxy limitation + quota), so a follow-up live UI pass is still worth doing when convenient.

- **Last updated:** 2026-09-14 — Turn-Time Region/Location/Area Logic + Live QC Pass #4 (`src/types.ts`, `src/api/xmlTurnContract.ts`, `src/lib/xmlTurnParser.ts`, `src/lib/locations.ts`, `src/lib/jitContext.ts`, `src/lib/codex.ts`, `src/App.tsx`, `src/screens/Chronicle.tsx`):
  - **What changed:** Investigated a direct question — does Region → Location → Area generation/handling actually work end-to-end, or only at Tale creation? Confirmed via code reading that Tale Weaving's own generation was already correct, but turn-by-turn play had a real gap: the per-turn context never told the model a location's own areas or region existed, there was no XML channel to move between areas, and any location created mid-play always defaulted to `region: 'Unmapped'` with no coordinates. Implemented the fix:
    1. `Player.areaId?: string` tracks the player's position among the current location's own named areas, auto-cleared whenever `locId` itself changes.
    2. `<turn>` gains `area`/`areadisp`/`areadesc` (move between/discover/step out of a location's own areas, `area="none"` sentinel to leave one) and `region`/`regiondisp`/`mapx`/`mapy` (assign a genuinely NEW location a real region + 0-100 coordinates, only on the turn it's first introduced) — both economy-gated the same "only when it changed" way `loc_desc` already is.
    3. `jitContext.ts` surfaces "Areas within X"/"Current Area" for the player's current location, a "Known Regions" line, and region+xy alongside each other known location that has them — so the model has something concrete to reason proximity from when placing a new location, grounded in the Tale's own real-world source geography when a Lore Accuracy Contract is active, or Earth-like region/country proportions otherwise.
    4. `lib/locations.ts`'s `ensureLocation()` accepts optional region info, stamped only at creation, never on a later revisit.
    5. **Live QC Pass #4 caught and fixed two real bugs** before considering this done: (a) the model reached for a brand-new `loc_id` instead of `area=""` when moving into one of the current location's own pre-authored areas (e.g. "Riders Quadrant" → its own "The Mess Hall"), forking a duplicate top-level location — fixed via a client-side self-heal in `App.tsx` (redirect through `effective_*` values) plus a concrete worked example added to the grammar's rule text, and verified fixed live (a fresh generation of the identical scenario then got it right natively); (b) the separate `{{Term|loc}}` keyword-tag auto-registration path had the same fork one level down (a tag matching an existing *area* name, not a location name, wasn't caught by the existing dedup check) — fixed via a new `isKnownAreaName()` guard in `lib/codex.ts`.
    6. **Verified working correctly, live, no changes needed**: new-location region/coordinate assignment (the model correctly reused an existing plausible region — "The Outskirts & Borders" — instead of inventing a redundant one, with sensible non-degenerate coordinates for a "remote eastern outpost"); the write-once economy held client-side even when the model itself restated coordinates on a revisit turn (the original values were correctly preserved, not overwritten); area persistence across a revisit to an earlier-known location; and no duplicate regions/locations anywhere across the full 8-turn combined test sequence.
    7. Chronicle.tsx's desktop HUD now shows the player's current area name alongside their location, when set.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean after each fix. Live-verified via direct real-API calls against `gemini-3.5-flash-lite` (today's `gemini-3.5-flash` free-tier quota was already spent by earlier passes). Full transcripts and applied-state snapshots in `public/reports/` (`qc-turns5-transcript.json`, `qc-turns6-transcript.json`, `qc4-campaign-after-*.json`, `QC-Report-4.md`).

- **Last updated:** 2026-09-14 — Live QC Pass #3: Narrative Settings (`src/lib/xmlTurnParser.ts`, `src/api/xmlTurnContract.ts`):
  - **What changed:** Live-API tested (`gemini-3.5-flash-lite`) the just-shipped Narrative Settings feature: Point of View (First/Third), Narration Mode (Reactive/Immersive), the broadened `*asterisks*` Directive Override for narrative claims, and Tale Difficulty (Chill vs Extreme contrast). All four behaved correctly — First/Third stayed consistent with no pronoun bleed onto NPCs; Reactive left the player's own words untouched while Immersive polished raw input into full first-person prose, thought, and invented-but-consistent dialogue; the override honored a lore-contradicting claim (a venin-forged dagger) as established fact via a real `inv_add`, with genuine narrative consequence; Extreme produced two severe Condition Tags and left the outcome unresolved where Chill produced one mild tag and a clean rescue.
  - **Fixed (live-caught, unrelated to the new settings themselves but surfaced during this pass)**: the model wrote `<turn d="Day 1">` (prefixed with the word "Day") instead of the grammar's required bare integer — `reqNum()`'s `Number("Day 1")` is `NaN`, which discarded the entire `<sync>` block (an item pickup, a beat activation, an NPC update) for that turn, same failure shape as Pass #1's quest-stat bug. Fixed by stripping a leading `"day "` prefix before parsing (`xmlTurnParser.ts`) and clarifying the grammar's rule text (`xmlTurnContract.ts`).
  - **Observed, not fixed**: a `{{Term|id: X}}` malformed category code on one sampling attempt (retried cleanly, read as ordinary model variance, not a grammar bug); `[[Location|loc]]`/`[[NPC|npc]]` using the item double-bracket convention with a category suffix — already defended against by an existing fuzzy-strip in `richText.tsx`, so no fix needed.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Full transcript and per-test settings in `public/reports/qc-turns4-transcript.json` / `QC-Report-3.md`.

- **Last updated:** 2026-09-14 — Narrative Settings: POV, Narration Mode, Directive Override, Tale Difficulty, Prose Depth rename (`src/types.ts`, `src/api/turnContract.ts`, `src/lib/jitContext.ts`, `src/screens/Settings.tsx`, `src/App.tsx`, `src/lib/taleWeaving.ts`, `src/screens/TaleWeaver.tsx`, `src/api/providers/gemini.ts`):
  - **What changed:**
    1. Added three new per-campaign Narrative Settings, following the exact same dial pattern Prose Depth already used (a `Campaign` field + an always-emitted `jitContext.ts` line, editable in Settings → Gameplay): **Point of View** (`Pov`: `'first' | 'third'`), **Narration Mode** (`NarrationMode`: `'reactive'` — today's only prior behavior, the player's own words stand as-is — or `'immersive'` — the model polishes the player's raw input into their character's own words/thoughts/actions as one continuous scene), and **Tale Difficulty** (`TaleDifficultyKey`/`TALE_DIFFICULTIES` in `turnContract.ts`: Chill/Easy/Balanced/Hard/Extreme, each carrying its own outcome-leaning guidance sentence; Balanced's guidance describes today's actual default behavior, unchanged).
    2. Broadened the existing `*asterisks*` Player Statement Override (turnContract.ts rule 3a) to also honor narrative/world-steering claims (not just mechanical stat claims) — this Tale's own power system or realm constraints never block an asterisked narrative claim; it still never overrides rule 5's Mature Themes boundary or an active Lore Accuracy Contract's Strict Spoiler Boundary.
    3. Renamed `PROSE_DEPTHS.IMMERSIVE` → `PROSE_DEPTHS.EXPANSIVE` (same values, `MIN_TURN_OUTPUT_CEILING` and all prose mentions updated) — frees up the word "Immersive" for Narration Mode, the more prominent player-facing use of it, so the same word doesn't mean two different things in the same Settings tab. `Settings.tsx`'s `proseDepthKey` seed migrates a stored `'IMMERSIVE'` label to `'EXPANSIVE'` so an old save's picker still opens on the right tier.
    4. Every brand-new Tale defaults to Third Person / Immersive / Balanced (Original Mode and the Original-Mode seed path) or Third Person / Immersive / **Extreme** (Tale Weaving's `beginInspiredTale`, since Extreme is Tale Weaving's own tagged-Recommended default) — Narration Mode defaults to Immersive everywhere per explicit request. An *existing* save with these fields absent falls back to Third Person/Reactive/Balanced at both `jitContext.ts` read-time and the `Settings.tsx` picker's seed state (matching each other exactly) — it never opted into Immersive, so it can't be silently upgraded to a mode it never chose.
    5. Added a "Narrative Settings" card (Point of View / Narration Mode / Tale Difficulty, Extreme tagged ★ Recommended) to Tale Weaving's Story Arc phase, and a one-line recap in the Tale Overview modal's Arc section. **Caught and fixed during verification**: the Arc phase has an "empty state" early-return (before any beat/event/stake has been woven) that would have made these plain-preference controls unreachable until the player wove real content first — refactored the card into a shared `renderNarrativeSettingsCard()` helper rendered in both the empty-state and full-content branches, so it's always reachable regardless of Arc content.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright click-through (mobile viewport, 420×860): reached Tale Weaver's Story Arc phase with zero beats woven and confirmed the Narrative Settings card renders there (not just once beats exist); toggled Point of View/Narration Mode/Tale Difficulty and confirmed the Tale Overview modal's recap line reflected the live selection (`First Person · Reactive · Chill`); separately opened Settings → Gameplay from the Main Menu (no active Tale) and confirmed all three new controls render, toggle, and the Prose Depth picker's top option now reads "EXPANSIVE".

- **Last updated:** 2026-09-14 — Live QC Pass #2: Items, Skills, Discovery Reveals, Overrides (`src/api/taleWeaverContract.ts`, `src/lib/taleWeaverParser.ts`, `src/App.tsx`, `src/lib/inventory.ts`):
  - **What changed:** Continued the same Fourth Wing/Violet Sorrengail test Tale from Pass #1 (with Lilith/Xaden secretTruth backstory context injected), running a 20-turn live-API pass focused on item/skill generation, Codex Discovery reveal mechanics, and deliberate `*asterisk*`-override stress tests for duplicates and fog-of-war.
    1. **Fixed: story-driven Codex Discovery reveals were architecturally impossible for anything Tale Weaving itself marks hidden.** Pass #1's `hidden`/`teaser` fix for Locations/Factions/NPCs hardcoded `revealTrigger: 'manual'` (copying Lore's existing pattern), but `lib/discovery.ts`'s `matchesReveal()` never auto-resolves a `'manual'` trigger — only actual Codex CRUD does. So every hidden entry from Tale Weaving, Lore included, could only ever be revealed by the player manually editing it in the Codex, never by playing the story forward. Added optional `reveal_trigger`/`reveal_cond` (`flag`/`location_visit`/`npc_met`) to the grammar, parser, and Campaign-assembly for all four hidden-capable categories (`'manual'` stays the default when omitted); deliberately excluded `quest_complete` since `Campaign.quests` is always empty at Tale Weaving time. Also restructured Campaign-assembly to resolve all reveal conditions in one pass *after* every category is built, so a condition naming an entity from a later-built category validates correctly.
    2. **Found (documented in the grammar's rule text, not further code-fixed): `npc_met`/`location_visit` can't reveal one hidden thing via another.** Live-seeded a real `revealTrigger: 'npc_met'` on the already-hidden NPC "Tairnageon," then had the player's own words directly name him — the reveal never fired. Root cause: a hidden entity's id is never shown to the model (same "dormant is invisible" design as Narrative Events), so the model has no way to cite an id it was never told exists. `reveal_cond` for `location_visit`/`npc_met` must target something *not itself hidden*; use `flag` when the target should stay secret too.
    3. **Fixed: item id drift (plural/singular) silently broke inventory removal.** Live-caught: model added `healing_draught` via `inv_add`, then removed `healing_draughts` (plural) one turn later — the old exact-match-only lookup in `lib/inventory.ts` silently no-op'd on the miss, so the player's item count never actually decreased despite the narration saying otherwise, with no visible error. Added `resolveInventoryId()`: tries the id as given, then its plural/singular counterpart, before giving up (same "self-healing over silent failure" spirit as `lib/codex.ts`'s fuzzy dedup guard).
    4. **Verified working, no changes needed**: item-quantity claims via override (`inv_add` with correct `qty`, no duplicate ids); skill-duplicate avoidance (re-teaching an already-known skill correctly produced no second `skill_learn`); nicknames/aliases (casual "Vi"/"Riorson" in player text never drifted the model's actual `{{Term|npc}}` tags off the canonical id — recommend *not* adding a dedicated aliases field unless a future pass actually catches a nickname-driven duplicate).
    5. **Found (recommendation only, not implemented)**: no equivalent of Narrative Events' `<event_trip>` exists for Codex Discovery — no way for the model to explicitly say "this specific hidden thing was just revealed in the story" on an ordinary turn. A player discovering hidden rebel-faction sigils in-story correctly left that faction hidden in the Codex (per current design, since it had no `reveal_trigger`), which is technically correct but a real UX gap — a future `<reveal id="..." category="...">`-style tag, mirroring `event_trip`, would close it.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean after each fix. Live-verified via direct real-API calls against `gemini-3.5-flash` (until its free-tier quota was hit again) and `gemini-3.5-flash-lite` (the full 20-turn pass — all 20 turns completed, 0 skipped). Full transcripts saved under `public/reports/` (`qc-turns2-transcript.json`, `qc-turns3-transcript.json`, `qc3-campaign-after-turn-*.json`, `QC-Report-2.md`). The Pass #1 flagged security issue (hardcoded `DEFAULT_GEMINI_API_KEY` in `src/lib/store.ts`) remains unfixed — still only actionable by the user (key rotation).

- **Last updated:** 2026-09-15 — Tale Weaver Editorial Vellum Reskin + Mockup Mirroring Pass (`src/screens/TaleWeaver.tsx`, `src/index.css`, `src/screens/Chronicle.tsx`, `src/types.ts`, `src/lib/inventory.ts`, `src/lib/jitContext.ts`, `src/lib/bangCommands.ts`, `src/App.tsx`, `src/screens/Codex.tsx`):
  - **Off-Hand equip slot**: `EquipSlot` grew from `'weapon' | 'armor' | 'accessory'` to also include `'offhand'`, decoupled from `ItemType` via a new `EQUIP_SLOT_ITEM_TYPE` map (both `weapon`/`offhand` require an `ItemType` of `'weapon'`, letting a player dual-wield or carry a shield). Threaded through `lib/inventory.ts`'s `equipItem` validation, `lib/jitContext.ts`'s per-slot equipped-gear labels, `lib/bangCommands.ts`'s new `equipSlotAlias()` (normalizes `offhand`/`off-hand`/`secondary`/etc.), Codex's item-detail "Equip as Weapon / Equip as Off-Hand" choice, and a 4th gear row in Chronicle's desktop sidebar.
  - **Full 7-phase Tale Weaver reskin**: propagated Chronicle's existing light "Editorial Vellum" theme (previously Chronicle-only) across the entire Tale Weaving flow — all 7 phases, shared header/stepper/footer chrome, presets, image generation, and modals — guided directly by 5 pasted mockup HTML files (Phase 1/2 preset browsers + editors, Phase 3 editor). Root cause of most of the work being a token-cascade fix rather than a rewrite: `.parchment-surface` (originally documented "Chronicle only") was wrapped around the whole screen, and since the file's body content already used semantic Tailwind classes (`text-gold-primary`, `border-gold-accent/NN`, `text-ink`) rather than literal hex, this auto-flipped ~90% of the screen's styling — only literal dark hex backgrounds needed manual `sed` replacement. Also stripped `backdrop-blur` throughout (three modal scrims, the shared confirm dialog) to match the app's established flat/performance-mode visual language.
  - **Mockup-mirroring follow-up pass**, done after direct visual comparison against the mockups turned up several remaining departures:
    1. **Confirm dialog**: the shared `useConfirm()` hook's card (`.glass-panel`) paints an opaque dark gradient from `--td-surface`/`--td-surface-raised` in flat mode — tokens `.parchment-surface` never touched, since it only overrides ink/gold/rose. Previously worked around by re-pinning ink/gold back to *dark*-chrome values so the dark card stayed legible; now pins just the two surface tokens to light vellum hex instead, so the card itself renders light (matching the Save/Load preset modals' own `#fbf8f3` fill) without touching the shared hook or any other screen that reuses it.
    2. **Inputs/textareas**: single-line fields switched to `bg-white`, textareas to a subtler `bg-[#faf8f4]`, both `rounded-md` (was `rounded-lg`/`rounded-xl`) with `shadow-xs` + `focus:ring-1 focus:ring-gold-primary` added — matching the mockup's input/textarea visual distinction instead of a uniform cream fill with no focus ring anywhere.
    3. **Real collapsible accordions**: replaced the static `FieldSectionHeader` divider (a hairline rule + label, always-expanded) with `CollapsibleSection` — a genuine click-to-expand/collapse accordion with a rotating chevron — on the World Foundation and Protagonist manual-entry forms, matching the mockup's `aria-expanded` collapsible field groups rather than one flat always-visible column.
    4. **Footer nav**: Back/Exit and Next/Review Tale were separate floating pill buttons; now one grouped bar with hairline dividers between segments (kept the app's own established solid-gold CTA fill for Next, rather than the mockup's dark-obsidian fill, to stay consistent with the CTA convention already used everywhere else in this reskin).
    5. **Button typography**: removed `font-display` (Cinzel) from all ~45 `<button>` elements in the file so they fall back to the app's plain sans body font via `index.css`'s global `button { font-family: var(--font-sans) }` rule — matching the mockup's hierarchy of Cinzel for headers/section-labels only, plain sans for every interactive control.
    6. **Preset browser**: the Load Draft modal gained a search field (filters by preset/world/hero name), a preset-count badge, and a segmented Load/Delete button group per card (was a single button + separate trash icon), mirroring the mockup's World Presets browser card layout. Also fixed a contrast bug caught in the process: the Regions/NPCs count badges used light `blue-300`/`purple-300` text sized for dark chrome, unreadable on the new white cards — swapped for `blue-700`/`purple-700` on tinted light fills.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean after every change in both passes. Live Playwright walkthrough (mobile viewport) across all 7 phases, the confirm dialog, the Overview modal, and the Save/Load preset modals — screenshotted before/after each fix. Preset search/filter verified against two seeded presets in `localStorage`. Not done in this pass (tracked as follow-ups): Chronicle's own remaining dark/oversized popups and its desktop HUD reskin, and an optional tap-to-inspect zoom/pan viewer for Tale Weaver's generated images.

- **Last updated:** 2026-09-15 — Tale Weaver Desktop Layout: Persistent Phase-Pipeline Sidebar (`src/screens/TaleWeaver.tsx`):
  - **What changed:** Given a dedicated PC-layout mockup (a fixed top header, a persistent left "Story Pipeline" sidebar, a two-column form+portrait-studio workbench, a docked bottom action bar, and a fixed status footer), added the sidebar treatment at the `lg:` breakpoint: a fixed-width left rail (hidden on mobile/tablet, which keep the existing horizontal phase-stepper strip) showing all 7 phases with Roman numerals, a progress bar, and per-phase state — a gold checkmark once a phase has real content, a filled dot on the active phase, a lock glyph on phases not yet visited (`idx > maxVisitedIdx`) — plus a "Tale Overview" quick-link card pinned to the bottom. Clicking a sidebar phase reuses the exact same `handleJumpToPhase()` the mobile stepper already called, so the confirm-on-skip-ahead / confirm-on-jump-back behavior is unchanged — only how phases are presented changed, not the navigation rules themselves. Also widened the main content column's max-width at `lg:`/`xl:`/`2xl:` so it doesn't read as sparse floating in the extra width the sidebar leaves available.
  - **Layout bug caught and fixed before shipping**: putting the new `flex-row` split's classes directly on `<GlassScreen>`'s own `className` did nothing — `GlassScreen` only applies flex/height styling to its own OUTER wrapper div; the inner div that actually renders `{children}` carries no layout classes at all when the `fill` prop isn't passed (which `TaleWeaver.tsx` doesn't). Confirmed via a live DOM inspection showing the sidebar and content area stacked vertically with the content pushed ~1200px below the fold, rather than side-by-side. Fixed by giving the sidebar+content split its own dedicated wrapper `<div>` one level inside `GlassScreen`'s children, rather than relying on `GlassScreen`'s className.
  - **Deliberately not attempted**: relocating the mockup's per-phase "Portrait Studio" (a sticky right-column image generator next to each phase's form) — in this app, every entity image generator (World cover, Protagonist portrait, Region map, NPC portrait) already lives centrally inside the Tale Overview modal, not inline per-phase. Mirroring that specific mockup split would mean moving working, already-verified image-generation UI out of its modal rather than a pure layout/CSS change, so it was left as a flagged follow-up rather than done silently.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright screenshots at a 1440×900 desktop viewport across Phase 1 and Phase 2, confirming the sidebar's lock/checkmark/active-dot states update correctly when jumping phases via the sidebar itself. Separately confirmed at a 420×900 mobile viewport that the sidebar computes to `display: none` and the existing mobile layout is pixel-for-pixel unaffected.

- **Last updated:** 2026-09-15 — Tale Weaver: Header Load/Save Placement + Mockup Phase Indicator (`src/screens/TaleWeaver.tsx`):
  - **What changed:** Both the mobile and PC mockups place Load/Save Preset as a single bordered pill group in the top header, next to the "Tale Dives / Weaver Studio" breadcrumb — this app instead had them living in the stepper-bar row alongside the Overview button. Moved Load/Save up into the header (Overview stays in the stepper row, since it isn't a mockup concept). Also replaced the plain "Phase X: Label" line above each phase's content with the mockup's eyebrow-style indicator — a gold dot + "PHASE X OF 7" + "/" + the phase name, all caps, small-caps typography — instead of a single Cinzel line, applied universally (mobile and desktop both use the same phase content area).
  - **Bug caught and fixed while wiring this in**: the new Load/Save labels were copied using this file's existing `hidden xs:inline` pattern (already used elsewhere, e.g. the Weave button), but this project's Tailwind v4 setup defines no custom `xs` breakpoint — `xs:` is simply not a real variant here, so `hidden xs:inline` resolves to permanently hidden at every viewport width, not "hidden below 400px" as the pattern implies everywhere else it's used. Caught via a live screenshot showing icon-only buttons with no visible label text even at 1440px wide. Fixed locally (plain always-visible `<span>Load</span>`/`<span>Save</span>`, verified to fit comfortably at both 420px mobile and desktop widths) — the same latent bug at the other three `hidden xs:inline` call sites in this file predates this session and was left alone rather than fixed silently as a drive-by.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright screenshots at both a 420×900 mobile viewport and a 1440×900 desktop viewport confirming the header's Load/Save pill group and the new phase-indicator eyebrow render correctly and fit without overflow at both sizes.

- **Last updated:** 2026-09-17 — Premium Image Cooldown Removed + New Vault "Generated Art" Gallery (`src/screens/TaleWeaver.tsx`, `src/screens/Codex.tsx`, `src/screens/MainMenu.tsx`, `src/components/VaultGeneratedArtView.tsx`):
  - **Premium generation no longer waits out the 62s cooldown**: `TaleWeaverImageGenerator` (Tale Weaver) and its near-identical counterpart in Codex.tsx both shared one global `useImageCooldown()` lockout across every image-gen call site, premium and non-premium alike. `handleGenerate` now only checks/starts the cooldown for `isPremium === false` — the regular (shared-key) path still throttles the same as before, but Premium (the player's own paid key) generates back-to-back. The Premium button's own disabled state and busy-spinner label were made explicit (`disabled={busy}` only, `busy ? 'Generating…' : 'Premium'`) in both files' "normal" and "prompt-edit" button rows, so it still can't be double-clicked mid-generation, just no longer artificially rate-limited afterward.
  - **Answered "are generated images saved in the campaign?"**: no — confirmed by re-reading `lib/imageStore.ts` and `App.tsx`'s `getFullBackupPayload`. A Codex entity (`LocationEntry.imageKey`, `NpcEntry.portraitKey`, `RegionEntry.mapImageKey`) only ever stores the string *key*; the actual image bytes live solely in this browser's own IndexedDB (`tale-dives-images` DB), never inside the Campaign JSON itself. Confirmed `getFullBackupPayload()` (used by both manual JSON export and auto-cloud-backup) only serializes `worlds/protagonists/campaigns/globalSlashCommands/apiSettings/uiPrefs` — no image blobs — so a restored backup on a different device/browser will show the right entities but with missing images (the keys resolve to nothing in that browser's own empty IndexedDB). This is a real portability gap worth a future pass (e.g. bundling blobs into the export as base64, or a "re-generate missing images" affordance) but out of scope for this pass — flagged, not fixed.
  - **New "Generated Art" gallery**, per request to surface AI-generated images somewhere browsable: the existing Vault → Art tab (`VaultArtGalleryView.tsx`) turned out to be unrelated static-wallpaper showcase content (the app's own bundled title-screen art), not a gallery of gameplay-generated images — so Art became a small two-way toggle (`Generated` | `Wallpapers`, Generated made the default/primary) rather than replacing existing content. New `VaultGeneratedArtView.tsx` walks every Tale's own Codex dicts (`locations[].imageKey`, `npcs[].portraitKey`, `regions[].mapImageKey`) to find which Tales actually have generated art, in a lean two-level drilldown: a Tale list (only Tales with ≥1 image shown, each card's image count) → clicking one opens a category-tabbed grid (Locations / Cast / Region Maps) of thumbnails for that Tale alone → clicking a thumbnail opens a lightbox with prev/next navigation within that category. Each thumbnail resolves its own `imageKey` through the existing `useEntityImage()` hook (IndexedDB → object URL), so no new storage or indexing was needed — this is a pure read/browse view over data that already existed.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright pass (mobile viewport): seeded a fake Tale + IndexedDB blobs directly via `localStorage`/`indexedDB`, confirmed the empty state ("No generated images yet…") when no art exists, confirmed the Tale card and image count appear once art exists, drilled into the Tale → Locations tab → thumbnail → lightbox with prev/next, all rendering and wiring correctly. (One test-fixture wrinkle, not an app bug: a freshly-seeded fake campaign with an empty `log: []` was silently pruned by `App.tsx`'s existing "delete orphaned Tales with no turns yet" cleanup on landing at Main Menu — confirmed this is correct, pre-existing, intentional behavior once traced, not a regression from this change; the test fixture just needed a non-empty log.)

- **Last updated:** 2026-09-17 — Fix Quick Play's Black Strip Below the Content Card, Two-Part Root Cause (`src/screens/QuickPlay.tsx`):
  - **First pass (incomplete)**: `<GlassScreen ground="dark" className="flex flex-col h-full overflow-hidden">` never passed the `fill` prop, so `GlassScreen`'s inner wrapper (`lib/glassChrome.tsx`) never got the `flex-1 min-h-0 flex flex-col` that stretches it to the outer `h-dvh` shell. Added `fill`. This alone did NOT fix it — a live screenshot at a real desktop viewport (1920×1080) still showed the black strip, catching the first pass as insufficient before calling it done.
  - **Real root cause, found by measuring computed heights directly in a live page** (`getBoundingClientRect()` on the actual DOM): even with `fill` passed, GlassScreen's outer div ends up with classes `h-dvh ... h-full ...` — QuickPlay's OWN `className` prop still carried a redundant `h-full` alongside `fill`'s `h-dvh`. Both are single-class selectors of equal specificity, so whichever rule Tailwind emits later in the compiled stylesheet wins the cascade — and `h-full` (`height: 100%`) won over `h-dvh`. Per CSS's percentage-height resolution rules, `height: 100%` against a parent whose own height comes only from `min-height` (the `motion.div` wrapper in `App.tsx` is `min-h-dvh`, not `h-dvh`) is not resolvable and computes to `auto` — so the element collapsed to its own content's height (measured: 561px of 1080px) instead of the full viewport, exposing the dark `ground="dark"` canvas below it. Fixed by dropping the now-redundant `h-full` from QuickPlay's own `className`, leaving `GlassScreen`'s own `fill`-driven `h-dvh` as the only height declaration on that element.
  - **Verified the actual fix, not just the code change**: measured the element's real rendered height in a live headless-browser pass before (561px, reproducing the reported bug exactly) and after (1080px, full viewport) the second fix, at the same 1920×1080 viewport the user's own screenshot was taken at — the first "fix" alone had passed a narrower manual check but hadn't caught this.
  - **Not touched**: `TaleWeaver.tsx` copies the same `GlassScreen ground="dark" className="... h-full ..."` pattern (this file's own code comment says QuickPlay was copied from there) and lacks `fill` too — same latent gap, masked there because Tale Weaver's phases are naturally tall enough to fill the viewport on their own. Left alone since nothing reported it broken there; worth revisiting if a short Tale Weaver phase ever shows the same black strip.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright checks at both a 1380×769 and a 1920×1080 viewport (matching the user's own reported resolution) confirmed the content now fills the full viewport height with no residual black gap below it.

- **Last updated:** 2026-09-18 — Tale Weaving: Duplicate NPC/Entity Forking + Dropped Gender + Hardcoded "They" Pronoun (`src/App.tsx`, `src/lib/jitContext.ts`), diagnosed from a real session payload (a Fourth Wing/Violet Sorrengail Tale):
  - **Duplicate Codex entries from a Tale-Weaving-created Tale, traced to an id-normalization mismatch**: the debug payload showed a live, reproducible case — "General Lilith Sorrengail" appearing twice in the Codex, once as the fully-detailed seeded entry (`general_lilith_sorrengail`) and once as a thin `auto`-created duplicate (`lilith_sorrengail`) the moment gameplay turns started referencing her. Root cause: every turn-time entity update (`npc_mem_up`, `loc_update`, `fac_rep`, ...) runs the given id through `slugify()` (`lib/slug.ts`), which deliberately strips a leading title word ("General"/"Captain"/"Lord"/...) so different phrasings of the same name converge on one id — but Tale Weaving's own merge (`App.tsx`'s `beginInspiredTale`) stored `regions`/`locations`/`factions`/`npcs`/`lore` under the model's raw, un-slugified Tale-Weaving-time id verbatim (`npcs[n.id]`, etc.), unlike World Seeding's merge (`lib/seeding.ts`), which already slugifies at creation (`slugify(n.id) || slugify(n.name)`). So a Tale-Weaving id that happened to retain a title word (here, the model itself chose `general_lilith_sorrengail` during the Cast of Characters phase) diverged the instant a later turn referenced her by that same id — `slugify()` strips "general" turning it into `lilith_sorrengail`, forking a second Codex entry rather than updating the original. Fixed by slugifying all five entity categories at Tale-Weaving merge time, matching `lib/seeding.ts`'s existing convention exactly (including re-deriving `locationRegionId` cross-references through the same slugify call) — verified the exact reported id pair (`general_lilith_sorrengail` / `lilith_sorrengail`) now converges to the identical slug (`lilith_sorrengail`) either way.
  - **Protagonist's `gender` silently dropped for every Tale-Weaving-created Tale**: the same payload's "Protagonist Identity" context line had no "Gender:" field at all for Violet, despite Tale Weaving's own grammar (`taleWeaverContract.ts`) requiring the model to supply `gender="male|female"` on the `<protagonist>` tag, and the parser (`taleWeaverParser.ts`) already reading it into `accumulated.protagonist.gender`. Traced to `beginInspiredTale`'s `player: Player = {...}` construction (`App.tsx`) copying `name`/`background`/`personality`/`motivation`/`physicalTrait`/`secret` from `protagonistData` but never `gender` — so `jitContext.ts`'s `player.gender && ...` check was always false for a Tale-Weaving Tale specifically (Original Mode's own `beginCampaign` path was unaffected). Fixed by adding the missing `gender: protagonistData.gender` line.
  - **Hardcoded "they" third-person pronoun, overriding canon**: compounding the above, `jitContext.ts`'s Point of View context line hardcoded the literal instruction `Third Person ("they")` regardless of the protagonist's actual established gender — so even once `player.gender` is populated, the turn prompt was still forcefully telling the model to use "they/their" for every sentence (matching the reported bug: Violet — canonically female, "she/her" — narrated with "their head", "their boots" throughout Turn #2). This is a real, confirmed correctness bug, not a style choice: the instruction actively fought the model's own correct canon knowledge rather than deferring to it. Fixed to defer to the protagonist's established gender (surfacing it inline when known) and fall back to "they" only when no gender has been established at all — matching how every other "restate the established fact so it can't drift" line in this file already works (heldWeapon, wornArmor, personality, ...).
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Verified the id-convergence fix with the exact reported id pair via the project's own `slugify()` logic (both `general_lilith_sorrengail` and `lilith_sorrengail` now resolve to the same key). Not re-run against the live API in this pass (diagnosed and fixed from the pasted session payload directly) — worth a live QC pass on a fresh Tale-Weaving-created Fourth Wing Tale to confirm no duplicate Codex entries and correct she/her narration end-to-end.
  - **Follow-up, confirmed from a longer 11-turn payload of the same Tale**: the duplicate ids and "they" pronoun were still visible in every turn of that later payload too — but it's the *same pre-existing Tale* the fix above doesn't retroactively repair (its `npcs` dict already has the forked entries baked in, and its `player.gender` was already permanently dropped at creation, before this fix existed). The code fix is correct and applies to any Tale-Weaving Tale created from now on; it does not self-heal an already-corrupted save. Flagged, not built in this pass: a one-time load-time migration (matching this codebase's existing `sanitizeWorldMasterFlag`-style "self-heal existing corrupted data on load" convention) to merge duplicate-by-slugified-id Codex entries in an existing save, if that's wanted for already-affected Tales.

- **Last updated:** 2026-09-18 — Raw `<plan>` Tag Leaking Into Narrative Text + Mobile Enter-to-Newline (`src/lib/xmlTurnParser.ts`, `src/screens/Chronicle.tsx`), both caught from the same longer session payload:
  - **`<plan>...</plan>` shown verbatim in the parchment**: the contract asks the model to place `<plan>` (its own private craft notes) *before* `<nar>`, and `parseXmlTurnResponse`'s narrative extraction (`raw.match(/<nar>([\s\S]*?)<\/nar>/)`) relied entirely on that ordering — nothing ever explicitly stripped `<plan>` content, it just normally fell outside the regex's capture. A live-caught turn had the model nest `<plan>...</plan>` *inside* `<nar>...</nar>` instead, so the extracted narrative literally included the raw `<plan>` XML block, and it rendered straight into the story text the player reads. Fixed by stripping any `<plan>...</plan>` block from the extracted `<nar>` text regardless of where the model actually put it — the same "tolerate a stray/misplaced tag rather than trust the model's formatting perfectly" posture this parser already uses for an unclosed tag from a token-cutoff response. Verified directly against the exact reported turn's raw text: the `<plan>` block is now stripped and only the intended prose remains.
  - **Mobile/tablet Enter key was sending the action instead of adding a newline**: Chronicle's action textarea treated any bare Enter (no Shift) as "send", which is correct for a physical keyboard's Shift+Enter convention but wrong for an on-screen mobile/tablet keyboard, where there's no comfortable way to hold Shift — so every Enter press on a phone/tablet fired the turn immediately instead of starting a new line. Fixed by skipping the send-on-Enter behavior (letting the browser's default newline-insert happen) whenever `('ontouchstart' in window) || matchMedia('(max-width: 1024px)').matches` — the same touch/small-screen detection `GlassScreen` already uses elsewhere in this app — so only a genuine desktop keyboard still sends on Enter; the Send button remains the only way to submit on mobile/tablet.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean. Live Playwright pass: a touch-enabled 420×900 mobile context typed "Line one", pressed Enter, typed "Line two" and confirmed the textarea held `"Line one\nLine two"` (not sent); a non-touch 1440×900 desktop context typed an action, pressed Enter, and confirmed the textarea cleared (sent), matching prior desktop behavior exactly.

- **Last updated:** 2026-09-19 — Turn-Zero Placeholder Text Changed to "Prologue Initiating..." (`src/screens/Chronicle.tsx`):
  - Chronicle's story log renders an italic placeholder on the parchment while `log.length === 0` — the state a Tale is in during its very first (Prologue) generation, before any turn has landed. Changed the copy from "No turns yet. Type an action below to begin." to "Prologue Initiating..." per direct request; confirmed via grep this string occurs exactly once in the codebase, so no other empty-state copy was affected.
  - **Verification**: `npx tsc --noEmit` and `npm run build` clean.
